# 체인지로그 (CHANGELOG)

온그린(On Green) 앱의 모든 주요 변경 사항을 이 파일에 기록합니다.

규칙은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/) 형식을 따르며,
**기능이 추가될 때마다 버전을 올리고(`app.js`의 `APP_VERSION`) 여기에 항목을 남깁니다.**

> **버전 두 종류 주의**
> - `APP_VERSION` (`app.js`): 프런트엔드 앱 버전. 이 체인지로그가 추적하는 값이며 기능 추가마다 올립니다.
> - `API.VERSION` (`api.js`): 서버(Apps Script)와의 통신 동기화용 버전. 백엔드를 함께 고칠 때만 백엔드 `VERSION`과 맞춰서 바꿉니다.

버전 표기: `vMAJOR.MINOR.PATCH`
- **MAJOR** — 큰 구조 변경 / 호환이 깨지는 변경
- **MINOR** — 새 기능 추가
- **PATCH** — 버그 수정 · 소소한 개선

---

## [v12.45.1] - 2026-09-22

### 수정됨
- **홈 화면 추가(재설치) 후 로그인 시 "인터넷 연결을 확인해주세요 (NET)" 오류**: `sw.js`(통과형 서비스 워커)가 같은 출처(GitHub Pages)뿐 아니라 다른 출처(Apps Script API) 요청까지 전부 가로채고 있었습니다. Apps Script `/exec` 엔드포인트는 GET·POST 모두 `script.googleusercontent.com`으로 302 리다이렉트하는데, 본문(body)이 있는 POST 요청(로그인 등)이 서비스 워커에 가로채인 채 교차출처로 리다이렉트되면 브라우저가 본문을 재전송하지 못해 `fetch()`가 실패하는 제약이 있습니다. 이 예외가 `callAPI()`에 의해 전부 "NET(네트워크 단절)"로 뭉뚱그려 표시되어, 실제로는 서버나 인터넷 연결과 무관한데도 로그인이 막혔습니다. `sw.js`가 **같은 출처 요청만** 가로채고 다른 출처(API) 요청은 건드리지 않도록 수정했습니다. 홈 화면 추가(WebAPK 설치 인식용 fetch 핸들러 존재)는 그대로 유지됩니다.

> 프런트 전용 변경(서비스 워커)입니다. 백엔드 `Apps_Script.gs`를 건드리지 않았으므로 `API.VERSION`은 그대로 두었습니다.

---

## [v12.45.0] - 2026-09-22

### 수정됨
- **앱 아이콘 테두리 완전 제거**: v12.44.0에서 픽셀 단위로 모서리를 손보는 방식으로 고쳤는데도 실기기에서 테두리가 남아 있었습니다(원인: 그 방식 자체가 모서리를 "잘라 덮는" 방식이라 원본의 부드러운 그림자 경계와 만나는 지점에 또 다른 희미한 경계선이 생겼습니다). 이번엔 방식을 바꿔, 강아지·공·글자만 원본에서 정확히 오려내고(배경과 색이 비슷한 눈·코까지 구멍이 안 뚫리게 처리) 배경은 처음부터 새로 그린 매끄러운 초록 그라데이션 위에 합성했습니다. 이제 모서리·테두리 어디에도 원본의 어두운 배경 흔적이 전혀 남지 않습니다. `icon-192/512.png`, `icon-maskable-192/512.png`, 파비콘·apple-touch-icon 전부 재교체.

> 프런트 전용 변경(이미지 자산)입니다. 백엔드 `Apps_Script.gs`를 건드리지 않았으므로 `API.VERSION`은 그대로 두었습니다.

---

## [v12.44.0] - 2026-09-22

### 수정됨
- **앱 아이콘의 이중 테두리 버그 수정**: v12.43.0에서 바꾼 아이콘이 실기기 홈 화면에서 진한 초록 테두리(원본 이미지 자체에 배경 프레임이 섞여 있던 흔적)가 비쳐 보이는 문제가 있었습니다. 원본 이미지에서 실제 아이콘 내용(강아지·글자)만 잘라내고 남은 모서리 그림자를 픽셀 단위로 정리해서, 배경이 끝까지 초록으로 꽉 차도록 다시 만들었습니다. `icon-192/512.png`, `icon-maskable-192/512.png`, 파비콘·apple-touch-icon 전부 재교체.

> 프런트 전용 변경(이미지 자산)입니다. 백엔드 `Apps_Script.gs`를 건드리지 않았으므로 `API.VERSION`은 그대로 두었습니다.

---

## [v12.43.0] - 2026-09-22

### 변경됨
- **앱 아이콘 교체**: 골프 옷차림으로 서 있던 마스코트를, 골프공을 물고 걷는 새 일러스트로 바꿨습니다. `icon-192.png`·`icon-512.png`·`icon-maskable-192.png`·`icon-maskable-512.png`를 새 이미지로 교체하고(마스커블 버전은 원형으로 잘려도 강아지·글자가 안 잘리도록 안전 여백을 둬서 다시 만들었습니다), `index.html`에 인라인으로 박혀 있던 브라우저 탭 파비콘·iOS 홈 화면 아이콘(180×180)도 같은 이미지로 함께 갱신했습니다.

> 프런트 전용 변경(이미지 자산)입니다. 백엔드 `Apps_Script.gs`를 건드리지 않았으므로 `API.VERSION`은 그대로 두었습니다.

---

## [v12.42.0] - 2026-09-21

### 추가됨
- **파3 티샷 결과에 "러프" 추가**: 지금까지 파3은 온그린·벙커만 고를 수 있었는데(벙커는 v12.38.0에서 이미 추가), 러프도 놓친 결과 중 하나로 고를 수 있게 했습니다. GIR은 이 선택과 무관하게 "온그린까지 타수"로만 계산되므로 기존 계산에는 영향이 없습니다.

### 변경됨
- 사용 설명서의 "티샷 결과" 항목을 파3에서도 러프·벙커를 고를 수 있다는 내용으로 갱신했습니다.

> 프런트 전용 변경(UI)입니다. 백엔드 `Apps_Script.gs`를 건드리지 않았으므로 `API.VERSION`은 그대로 두었습니다.

---

## [v12.41.0] - 2026-09-21

### 추가됨
- **"코스평균대비" 칩을 통계 화면 밖에도 노출**: v12.40.0에서 통계→라운드별 목록에만 있던 칩을 라운드 탭(홈)의 최근 라운드 목록과, 라운드를 눌렀을 때 나오는 상세 화면 상단(기본 정보) 두 곳에도 추가했습니다. 계산 기준은 동일(그 골프장 내 다른 라운드 평균 대비, 자기 자신 제외).
- **통계 → 스코어 탭 "핵심 지표"에 평균 멀리건·평균 OB·평균 해저드 카드 추가**: 라운드가 여러 개면 "평균", 1개면 그냥 숫자로 표시됩니다(기존 평균 스코어 등과 같은 표기 규칙).
- **스코어 입력 화면에 "티샷 외 해저드"·"티샷 외 OB" 카운터 추가**: 온그린까지/퍼팅 입력 아래에 +/- 스테퍼 2개를 새로 뒀습니다. 티샷 결과 칩(해저드/OB)은 "첫 샷"만 기록하는데, 어프로치 등 다른 샷에서 난 해저드·OB는 지금까지 기록할 방법이 없었습니다. 이 값은 스코어 계산에는 관여하지 않고(이미 "온그린까지 타수"에 반영돼 있음) 원인 분석용 기록으로만 저장됩니다(`xhzArr`/`xobArr` 필드 신설, 이번 버전부터 입력하는 라운드만 기록됨).
- **아이언 손실 타수를 원인별로 분해**: 위 카운터를 입력한 홀이 있으면, 라운드 상세·통계의 "🎯 아이언·웨지 정확도" 카드에 아이언 손실 타수 중 "벌타(티샷 외 해저드·OB)로 인한 손실"과 "거리감·클럽 선택 등 나머지 손실"을 나눠 보여줍니다(홀당 벌타 추정 타수는 아이언 손실 초과분을 넘지 않도록 잘라서 더합니다). 카운터를 안 쓴 홀만 있으면 기존과 똑같이 표시됩니다.

> 프런트 전용 변경(UI)입니다. 백엔드 `Apps_Script.gs`를 건드리지 않았으므로 `API.VERSION`은 그대로 두었습니다.

---

## [v12.40.0] - 2026-09-21

### 추가됨
- **통계 → 추세·기록 탭에 "⛳ 자주 가는 골프장" 막대그래프 추가**: 골프장별 방문 횟수·비율(%)·평균 스코어를 방문 횟수가 많은 순서로 보여줍니다. (코스 이름이 길어 도넛보다 막대그래프가 더 읽기 좋아 막대그래프로 구현)
- **통계 → 라운드별 목록에 "코스평균대비" 칩 추가**: 각 라운드 카드에 그 골프장에서의 다른 라운드 평균 스코어 대비 몇 타인지(▼좋음/▲나쁨)를 보여줍니다. 라운드 상세의 "이 골프장 이전 기록과 비교"와 같은 기준(자기 자신은 제외)으로 계산합니다. 그 골프장을 처음 갔으면 표시되지 않습니다.

### 변경됨
- `courseCompareHTML()`이 쓰던 "같은 골프장의 자신을 뺀 다른 라운드 평균" 계산을 `courseOtherAvg()`로 뽑아내 새 칩과 공유하도록 정리했습니다(동작 변화 없음).

> 프런트 전용 변경(UI·계산 로직)입니다. 백엔드 `Apps_Script.gs`를 건드리지 않았으므로 `API.VERSION`은 그대로 두었습니다.

---

## [v12.39.0] - 2026-09-21

### 추가됨
- **티샷 안정성 카드에 원형그래프(도넛) 추가**: 페어웨이(온그린)·러프·벙커·해저드·OB 5가지 개수와 비율을 한눈에 보여줍니다(멀리건은 페널티가 없어 제외).
- **스코어 미입력 홀 자동 파 처리**: 홀에서 아무것도 안 만지고 "저장 · 다음 홀 →" / "저장 · 완료"를 바로 눌러도 그 홀은 파로 자동 확정되고 진행됩니다. 이미 값을 입력한 홀은 그대로 유지됩니다.
- 위 자동 확정 덕분에 **18번 홀에서 아무 입력 없이 "저장 · 완료"를 눌러도** 18홀이 모두 채워져 라운드 상세 기록 화면으로 바로 이동합니다(기존에도 18홀이 다 채워지면 자동 이동했지만, 마지막 홀 미입력 시 이동하지 않던 문제).

### 수정됨
- **골프장 코스 수정 중 이름을 전체 선택(길게 눌러 드래그)하면 모달이 닫히고 골프장 목록으로 튕기던 버그 수정**: 모달 뒤 배경을 눌러야 닫히는 로직이 클릭이 "끝난" 지점만 보고 있어서, 입력창 안에서 시작한 텍스트 선택 드래그가 배경 위에서 끝나면 배경을 누른 것으로 오인해 모달을 닫아버렸습니다. 이제 드래그의 시작 지점도 배경이어야 닫히도록 고쳤습니다.
- 통계 화면의 **"부서별" 탭 이름을 "구간별"로 변경**했습니다(내용은 동일 — 티샷 안정성·드라이버·아이언·숏게임·퍼팅).
- 통계 → 구간별 탭 맨 위에 **"📉 손실타수 한눈에 보기"** 막대그래프를 추가했습니다. 드라이버·아이언·웨지·숏게임·퍼팅 손실 타수(라운드 평균, 이미 각 카드에 있던 숫자를 재사용)를 같은 기준으로 놓고 큰 순서로 비교합니다. 드라이버는 표본이 적으면(페어웨이 지킨/놓친 홀이 각각 5개 미만) OB·해저드 페널티 타수로 추정한 값에 `*` 표시가 붙습니다.

> 프런트 전용 변경(UI·계산 로직)입니다. 백엔드 `Apps_Script.gs`를 건드리지 않았으므로 `API.VERSION`은 그대로 두었습니다.

---

## [v12.38.0] - 2026-09-21

라운드 후 통계를 5개 부서(티샷 안정성·드라이버·아이언·숏게임·퍼팅)로 다시 짰습니다. 라운드 상세와 통계→"부서별" 탭이 같은 계산을 공유합니다.

### 추가됨
- **🚩 티샷 안정성 (Off-the-Tee · 파3~5 모두)**: 티샷 페널티율(OB+해저드 홀 ÷ 전체 홀), OB·해저드·멀리건 홀 수, 안전 미스(러프·벙커, 페널티 없음) 개수·비율.
- **🚗 드라이버 안정성 (Driver · 파4·5)**: FIR%, 드라이버 손실 타수(기존 계산 재사용).
- **🎯 아이언·웨지 정확도 (Approach)**: GIR%, 아이언 손실 타수(기존 계산 재사용), **온그린 3타↑ 비율(파4 기준)을 페어웨이 지킨 홀·놓친 홀로 나눠 분석** — 지켰는데도 비율이 높으면 아이언·웨지 거리감 문제, 놓쳤을 때만 높으면 드라이버가 원인임을 알려줍니다.
- **⛳ 숏게임 능력 (Short Game)**: 스크램블링%, 숏게임 손실 타수(기존 계산 재사용).
- **🍩 퍼팅 효율성 (Putting)**: PPR(총 퍼팅)·홀당 평균·GIR 시 평균 퍼트·3퍼트 이상 비율·1퍼트율·퍼팅 손실 타수·퍼팅 분포.
- **티샷 결과에 "벙커"를 추가**했습니다(파3·4·5 모두). 분류: 페어웨이=FIR 반영 · 러프·벙커=FIR 미반영(페널티 없음) · 해저드·OB=페널티 · 멀리건=페널티 없음.
- **러프/벙커를 이제부터 구분해서 저장**합니다(`missArr` 필드 신설). 페널티 계산에는 안 쓰고, 티샷 안정성의 미스 유형 집계에만 씁니다. **이번 버전부터 입력하는 라운드만 구분되고, 과거 기록은 "미상"으로 표시됩니다.**

### 변경됨
- 라운드 상세의 **"🎯 손실 타수, 어디서 났나"**와 **"퍼팅·쇼트게임"** 섹션을 삭제하고, 위 5부서 통계로 흡수했습니다. "고칠 순서" 랭킹·팁 문구는 없어지고, 각 부서 카드 안에 손실 타수 숫자로 대신합니다.
- 통계 화면의 "숏게임·퍼팅" 탭을 **"부서별"**로 이름을 바꾸고 5부서 통계를 전부 넣었습니다("파 종류별"은 그대로 유지).
- 라운드를 다시 열 때 러프/벙커/파3 온그린 선택 칩이 더 정확히 복원됩니다(`deriveTeeSel()` 개선).
- `analyze()` 함수를 5부서 통계에 필요한 값을 전부 계산하도록 다시 짰고, 더는 안 쓰이는 `extraStats()`의 퍼팅 분포·FIR/GIR 평균 계산을 정리했습니다(퍼팅 분포는 이제 `analyze()`가 계산).

> 프런트 전용 변경 — `API.VERSION`은 그대로 둡니다. 저장 스키마에 `missArr` 필드가 새로 추가됐지만 기존 필드는 안 바뀌었고, 이 필드가 없는 과거 라운드도 정상 동작합니다(러프/벙커만 "미상"으로 표시).

---

## [v12.37.0] - 2026-09-21

### 추가됨
- **티샷 결과에 "벙커"를 추가했습니다.** 파3·4·5 모두 선택할 수 있습니다. 분류: **페어웨이**=FIR 반영 · **러프·벙커**=FIR 미반영(페널티 없음) · **해저드·OB**=페널티(손실 타수 집계) · **멀리건**=페널티 없음. `setTee()`의 fir/tp/mulli 계산 로직은 그대로이며(벙커는 러프와 동일하게 fir=false·tp=0·mulli=0), 새 칩만 추가했습니다.
- ✅ **확인한 사실**: 파3의 GIR은 티샷 결과 칩(온그린/벙커 등) 선택과 **무관**하게, 오직 "온그린까지 타수"(≤1이면 GIR)로만 계산됩니다. 코드로 직접 확인했습니다(`recalcHole()`). 그래서 벙커를 추가해도 GIR 계산은 손댈 필요가 없었습니다.

### 변경됨
- **라운드 상세 첫 화면의 "멀리건/OB/해저드"를 나눴습니다.** 멀리건은 페널티가 없으므로 별도 "멀리건" 카드로 분리했고, 나머지는 "**티샷 패널티**"로 묶어 OB·해저드 발생 횟수를 각각 보여줍니다(계산식은 그대로: OB 1회=2타·해저드 1회=1타).
- 추세 탭·통계 설명서의 "티샷손실"을 "**티샷패널티**"로 이름을 맞췄습니다(라운드 상세와 같은 이름을 쓰도록 통일).
- 홀 상세의 "러프 (사고 없음)" 문구를 "러프/벙커 (사고 없음)"로 고쳤습니다 — 저장된 데이터만으로는 러프와 벙커를 구분할 수 없어서(둘 다 FIR 없음·페널티 없음으로 동일하게 저장), 둘 다 아우르는 표현으로 바꿨습니다.

> 프런트 전용 변경 — `API.VERSION`은 그대로 둡니다. 저장 스키마(fir/tp/mulli 배열)는 안 바뀌었고, 벙커는 러프와 같은 방식으로 기록됩니다.

---

## [v12.36.0] - 2026-09-21

전체 코드 점검(버그·중복 코드·이름 불일치)에서 나온 4가지를 반영했습니다.

### 변경됨
- **"파대비"·"파 대비" 표기를 "오버파"로 통일했습니다.** 통계 화면만 "오버파"라 부르고 라운드 상세·스코어 입력 하단바·CSV 내보내기·공유 텍스트·설명서는 전부 "파 대비"라 불러서(설명서 안에서도 두 이름이 섞여 있었음) 하나로 통일했습니다. 계산 로직은 그대로입니다.
- **"내 평균 대비" 비교가 그 라운드 자신을 빼고 계산하도록 고쳤습니다.** 라운드 상세·통계→라운드별 목록의 🟢🟡🔴 FIR·GIR·퍼팅 색은 지금까지 "내 평균"에 그 라운드 자신까지 포함해서 비교했습니다(바로 옆 "이 골프장 이전 기록과 비교"는 원래도 자신을 뺐는데, 이 부분만 방식이 달랐습니다). 이제 항상 "그 라운드를 뺀 내 다른 라운드 평균"과 비교합니다.

### 제거됨
- **`analyze()` 함수의 안 쓰이는 계산을 정리했습니다.** 예전 🚦신호등 진단 기능이 쓰던 계산(survPct·adjFir·girPct·scrPct·puttAvg·threeAvg·girPuttAvg·퍼팅 분포·missGreen·scrSave·평균 스코어/오버파 등 16개 값)이 그 기능을 지운 뒤에도 안 쓰인 채 남아 있었습니다(다른 화면에서 이미 따로 계산 중이던 값과 완전히 중복). 실제 쓰이는 손실 타수 계산만 남기고 정리했습니다. 화면에 보이는 값은 전혀 바뀌지 않습니다.
- 통계 설명서에서 이미 없어진 "퍼팅 등급"을 언급하던 문장("3퍼팅 빈도가 퍼팅 등급을 정해요")을 지웠습니다.

> 프런트 전용 변경 — `API.VERSION`은 그대로 둡니다. 표기·죽은 코드 정리와 평균 비교 계산 하나만 고쳤고, 저장 데이터나 나머지 계산 로직은 그대로입니다.

---

## [v12.35.1] - 2026-09-18

### 변경됨
- **스코어 입력의 "탭해서 파 입력" 박스를 더 눈에 띄게 강조했습니다.** 아직 안 만진 홀의 "입력 전" 박스가 다른 회색 카드들과 구분이 안 됐는데, 초록 점선 테두리·초록 배경·👆 아이콘을 더해 "탭하면 된다"는 걸 바로 알아볼 수 있게 했습니다.

> 프런트 전용 변경 — `API.VERSION`은 그대로 둡니다. 스타일만 바뀌었고 동작(탭하면 파 확정)은 그대로입니다.

---

## [v12.35.0] - 2026-09-18

### 제거됨
- **관리자 설정의 "📊 분석 기준"(BENCH) 화면을 완전히 삭제했습니다.** 신호등 진단(🚦) 기능이 통계·라운드 상세에서 모두 없어지면서, 그 기준값(페어웨이%·GIR%·스크램블%·퍼팅/3퍼팅 임계값)을 쓰는 곳이 앱에 하나도 안 남아 있었습니다. 설정 화면의 "분석 기준" 섹션, 관리자 전용 수정 입력칸·저장 버튼을 없애고, 프런트에서 `getBench`/`setBench` 서버 호출도 더 이상 하지 않습니다.
- 안 쓰이게 된 `BENCH` 전역 객체, `benchFormulaHTML()`·`renderBenchSettings()`·`saveBench()` 함수, `api.js`의 `getBench`/`setBench` 메서드를 삭제했습니다.

> 프런트 전용 변경 — `API.VERSION`은 그대로 둡니다. 백엔드(`Apps_Script.gs`)의 `getBench`/`setBench` 핸들러 자체는 건드리지 않았습니다(프런트가 더 이상 호출하지 않을 뿐 백엔드 배포는 필요 없음). 드라이버·아이언·숏게임·퍼팅 손실 타수(⭐약점 우선순위)는 이 기준값과 무관한 별도 실측 계산이라 영향 없습니다.

---

## [v12.34.1] - 2026-09-18

### 수정됨
- **"큰 실수의 원인"이 세는 기준을 "더블보기 이상"에서 "블로업(트리플보기 이상)"으로 바로잡았습니다.** 실력 비율 카드의 "블로업/R" 수치와 정의가 서로 달라(하나는 더블보기↑, 하나는 트리플보기↑) 어긋나 있었는데, 둘 다 트리플보기 이상으로 통일했습니다. (통계·라운드 상세 공통)
- **"큰 실수의 원인" 좌측 지표 이름(티샷 사고·3퍼팅↑·그린 미스)이 여러 줄로 잘리던 것을 한 줄로 고쳤습니다.** 이름을 짧게 줄이고 칸 너비를 늘렸습니다. (통계·라운드별 공통)

> 프런트 전용 변경 — `API.VERSION`은 그대로 둡니다. 저장 데이터는 안 바뀌고 "큰 실수" 집계 기준과 화면 표기만 고쳤습니다.

---

## [v12.34.0] - 2026-09-18

### 변경됨
- **통계(전체) 탭에서도 "진단" 탭을 없앴습니다.** 🚦 신호등 카드와 그 안의 "약점 우선순위"(라운드 상세와 중복)를 정리했습니다. 이제 통계 하위 탭은 **스코어 · 숏게임·퍼팅 · 추세·기록** 3개입니다.
- **"정확도·퍼팅" 탭 이름을 "숏게임·퍼팅"으로 바꿨습니다.** (라운드 상세·통계 화면 공통)
- **추세 탭의 "티샷손실타수" 표기를 "티샷손실"로 줄여 세그먼트 버튼에서 두 줄로 안 잘리고 한 줄에 들어오게 했습니다.**
- 위 정리에 따라 더는 쓰이지 않는 `analysisHTML`(진단 신호등 카드 렌더러)과 관련 CSS(`.dgi*`)를 삭제했습니다. 사용 설명서·업데이트 소식·분석 철학 안내 문구도 없어진 화면을 가리키지 않게 함께 고쳤습니다.

> 프런트 전용 변경 — `API.VERSION`은 그대로 둡니다. 저장 데이터·계산 로직은 안 바뀌고 화면 구성과 표기만 정리했습니다.
> ⚠️ 참고: 관리자 설정의 "분석 기준값"(BENCH의 firGood·girGood·scrGood·puttGood 등 등급 임계값)은 이 신호등 진단 기능에서만 쓰였는데, 그 기능이 이제 화면에서 완전히 없어져서 그 설정값을 바꿔도 더 이상 앱 어디에도 반영되지 않습니다. 드라이버·아이언·숏게임·퍼팅 손실 타수(⭐약점 우선순위)는 이 임계값과 무관하게 실측값으로 계산되는 별도 로직이라 영향 없습니다.

---

## [v12.33.0] - 2026-09-18

### 변경됨
- **라운드 상세의 "🔍 이 라운드 분석" 토글을 없애고, 안에 있던 정보를 기본 화면에 바로 보이게 정리했습니다.**
  - "정확도·퍼팅" 탭 내용(파 종류별 · 퍼팅/쇼트게임 · 퍼팅 분포 — **스크램블링%도 여기 포함**)을 기본 화면에 그대로 옮겼습니다.
  - "진단" 탭에 있던 🚦 신호등 카드와 💊 오늘의 처방은 없앴습니다. 그 안의 "약점 우선순위"는 기본 화면의 "🎯 손실 타수, 어디서 났나"와 내용이 완전히 같아 중복이었어서, 그 섹션 하나만 남기고 정리했습니다.
  - 탭을 누를 필요 없이 한 화면에서 바로 다 보입니다.

### 추가됨
- **스코어 입력 중 "입력 전" 박스를 탭하면 파를 원터치로 입력합니다.** 온그린·퍼팅을 안 만졌을 때 표시되는 기본 미리보기 값이 항상 파와 같다는 점을 이용해, 그 박스를 탭하면 바로 그 값(파)이 확정 기록됩니다. 파 아닌 스코어는 기존처럼 온그린/퍼팅 +/- 버튼으로 조정합니다.

> 프런트 전용 변경 — `API.VERSION`은 그대로 둡니다. 저장 데이터·계산 로직은 안 바뀌고 화면 구성과 입력 방식만 정리했습니다.

---

## [v12.32.0] - 2026-09-18

### 변경됨
- **라운드 상세와 "🔍 이 라운드 분석"의 겹치는 내용을 정리했습니다.** "이 라운드 분석"의 "스코어" 탭을 없애고(분석은 이제 "정확도·퍼팅"·"진단" 두 탭), 그 탭에만 있던 정보(실력 비율 도넛·큰 실수 원인·전반/후반·타수 분포)를 라운드 상세 기본 화면에 바로 보이게 옮겼습니다. 탭을 안 눌러도 한 화면에서 다 보입니다. 기존 스탯 카드와 중복이던 "핵심 지표"(스코어·오버파·퍼팅·GIR·FIR)는 빠졌습니다(이미 위에 같은 값이 있었음).

> 프런트 전용 변경 — `API.VERSION`은 그대로 둡니다. 저장 데이터·계산 로직은 안 바뀌고 화면 배치만 정리했습니다.

---

## [v12.31.1] - 2026-09-18

### 변경됨
- **🎯 아이언 손실 타수 계산을 훨씬 간단하게 바꿨습니다.** "그린 지킨 홀 vs 놓친 홀 평균 비교"(드라이버 방식) 대신, **홀마다 "온그린까지 실제 타수가 정규타수(파−2)를 몇 타 넘겼는지"를 바로 더한 값**을 씁니다. 예: 파4에서 3타 만에 온그린했으면 정규타수(2타)보다 1타 초과 → 그 1타가 그대로 아이언 손실에 더해집니다. GIR 적중/미스 비교나 표본 크기를 따질 필요가 없어져 항상 정확한 값이 나옵니다.

> 프런트 전용 변경 — `API.VERSION`은 그대로 둡니다. 저장 데이터는 안 바뀌고 아이언 계산식만 더 단순해졌습니다.

---

## [v12.31.0] - 2026-09-18

### 변경됨
- **"손실 타수, 어디서 났나"의 아이언·숏게임·퍼팅 계산을 "홀 수" 대신 "실제 손실 타수"로 바꿨습니다.** 직전 버전에서 넣었던 "홀 수 그대로 반영" 방식이 실제 타수를 나타내지 못한다는 지적을 반영했습니다.
  - **🎯 아이언**: 그린을 지킨 홀과 놓친 홀의 "파 대비" 평균 차이 × 라운드당 놓친 홀 수 — 드라이버(티샷 손실)와 똑같은 방식입니다. 표본이 너무 적으면(그린 지킨/놓친 홀이 각각 5홀 미만) 예전처럼 놓친 홀 수로 대체합니다.
  - **⛳ 숏게임**: 그린을 놓치고 파를 못 지킨 홀들의 "파 초과 타수"를 그대로 더한 값(실측값이라 대체식 불필요).
  - **🍩 퍼팅**: 2퍼팅 기준 초과 타수를 그대로 더한 값(3퍼팅=1타, 4퍼팅=2타 …, 역시 실측값).
  - 🚗 드라이버는 기존 방식(티샷 손실 실측 또는 홀 수 대체) 그대로입니다.

> 프런트 전용 변경 — `API.VERSION`은 그대로 둡니다. 저장 데이터는 안 바뀌고 진단 계산식만 더 정확해졌습니다.

---

## [v12.30.5] - 2026-09-18

### 변경됨
- **🍩 퍼팅 손실 타수를 "3퍼팅 홀 수" 기준으로 바꿨습니다.** 기존엔 "평균 총 퍼팅 수가 기준값(33개)보다 얼마나 많은지"로 추정했는데, 이제 **그 라운드의 3퍼팅 홀 수를 그대로** 손실 타수로 씁니다. 이제 드라이버·아이언·숏게임·퍼팅 네 항목 모두 "각자의 실수 홀 수를 그대로 손실 타수로 쓰는" 방식으로 통일됐습니다.

> 프런트 전용 변경 — `API.VERSION`은 그대로 둡니다. 퍼팅 항목의 계산식만 바뀌었습니다.

---

## [v12.30.4] - 2026-09-18

### 변경됨
- **⛳ 숏게임 손실 타수에서 임의로 곱하던 ×0.5 가중치를 뺐습니다.** 이제 "그린을 놓치고 파를 못 지킨 홀 수"를 그대로 씁니다(예: 그린 놓친 10홀 중 4홀에서 파 세이브 실패 → 숏게임 손실 4.0타). 아이언·드라이버처럼 홀 수를 그대로 쓰는 방식으로 통일했습니다.

> 프런트 전용 변경 — `API.VERSION`은 그대로 둡니다. 숏게임 항목의 계산식만 바뀌었고 나머지(드라이버·아이언·퍼팅)는 그대로입니다.

---

## [v12.30.3] - 2026-09-18

### 변경됨
- **"손실 타수, 어디서 났나"의 🎯 아이언 계산 방식을 바꿨습니다.** 기존엔 "GIR% 가 기준값(40%)보다 얼마나 낮은지"를 근사식으로 추정했는데, 이제 **라운드당 그린을 놓친 홀 수를 그대로** 손실 타수로 씁니다(드라이버의 파4·5 홀 수 집계 방식과 같은 결로 맞췄습니다). 예: 그린을 5홀 놓쳤으면 아이언 손실은 +5.0타.

> 프런트 전용 변경 — `API.VERSION`은 그대로 둡니다. 아이언 항목의 계산식만 바뀌었고 나머지(드라이버·숏게임·퍼팅)는 그대로입니다.

---

## [v12.30.2] - 2026-09-18

### 변경됨
- **라운드 상세의 "M / TP" 표시를 "멀리건/OB/해저드" 세 숫자로 분리했습니다.** 뭉뚱그려 있던 숫자를 각각 무엇인지 바로 알 수 있게 했습니다.
- **"손실 타수" 카드를 "티샷 손실 타수"로 이름을 바꿨습니다.** 이 수치는 OB·해저드로만 계산되는(퍼팅·아이언 등은 안 들어가는) 값이라, 바로 아래 "손실 타수, 어디서 났나"(드라이버·아이언·숏게임·퍼팅 전체 분해)와 헷갈리지 않도록 구분했습니다.
- **🚗 드라이버 손실 타수 항목에 "파4·5 홀만 집계, 파3 제외" 안내를 추가했습니다.** 이 항목은 파4·5 홀의 OB/해저드만 반영하도록 원래부터 설계돼 있어서, 그날 사고가 전부 파3에서 났으면 "티샷 손실 타수"는 있는데 드라이버 항목은 0.0타로 보일 수 있습니다 — 계산은 그대로 두고 문구로 명확히 했습니다.

> 프런트 전용 변경 — `API.VERSION`은 그대로 둡니다. 계산 로직은 바뀌지 않고 표시 문구만 정리했습니다.

---

## [v12.30.1] - 2026-09-18

### 변경됨
- **스코어 입력 결과 배너·홀 상세에서 이글 이하를 세분화했습니다.** "이글 이하"로 뭉뚱그려 보여주던 것을 실제 타수 기준으로 정확히 구분합니다: 스코어 1타는 파와 무관하게 항상 <b>홀인원</b>, 파 대비 −3타 이하는 <b>알바트로스</b>, −2타는 <b>이글</b>로 표시합니다.

> 프런트 전용 변경 — `API.VERSION`은 그대로 둡니다. 저장 데이터는 바뀌지 않고 표시 이름만 세분화했습니다.

---

## [v12.30.0] - 2026-09-18

### 추가됨
- **파3 홀의 티샷 결과에 "온그린" 칩을 추가했습니다.** 파4·5는 페어웨이/러프가 있는데 파3은 그 개념이 없어 해저드·OB·멀리건뿐이었습니다 — 이제 파3에서도 티샷이 그린에 바로 올라간 성공을 표시할 수 있습니다.
- **18홀을 다 채우고 저장하면 그 라운드의 분석(라운드 상세) 화면으로 자동 이동합니다.** 덜 채운 상태로 중간 저장할 때는 기존처럼 입력 화면에 그대로 머뭅니다.

### 변경됨
- **"실력 비율 (홀 기준)"을 원형그래프(도넛 차트)로 표현했습니다.** 파 이하·보기·더블+ 세 구간은 서로 겹치지 않고 합쳐서 전체 홀 수가 되므로 원형그래프에 그대로 들어맞습니다. 버디%·블로업/R은 도넛에 안 들어가는 보조 지표라 그대로 숫자 카드로 남겨뒀습니다.

> 프런트 전용 변경 — `API.VERSION`은 그대로 둡니다. 저장 데이터 스키마도 바뀌지 않았습니다.

---

## [v12.29.0] - 2026-09-18

### 변경됨
- **스코어 입력 화면에서 "티샷 결과" 선택지 위치를 파3/4/5 탭 바로 아래로 옮겼습니다.** 온그린·퍼팅 입력보다 먼저 보이도록 순서를 바꿨습니다.

### 추가됨
- **라운드 상세에 "손실 타수, 어디서 났나" 분해 카드를 추가했습니다.** 그 라운드의 손실 타수가 드라이버(티샷)·아이언·숏게임(어프로치)·퍼팅 중 어디서 가장 많이 났는지 막대그래프 + 우선순위로 바로 보여줍니다. 기존 "🔍 이 라운드 분석 → 진단" 탭에만 있던 계산(`weaknessItems`/`weaknessHTML`)을 라운드 상세 화면에도 바로 노출한 것이라 새 계산식은 없습니다.

> 프런트 전용 변경 — `API.VERSION`은 그대로 둡니다. 저장 데이터 스키마도 바뀌지 않았습니다.

---

## [v12.28.0] - 2026-09-18

### 추가됨
- **"손실 타수" 지표 추가** — 티샷이 OB·해저드에 빠져 깎아먹은 타수를 숫자로 보여줍니다(OB 1회=2타, 해저드 1회=1타로 환산, 멀리건은 벌타가 없어 제외).
  - **라운드 상세**에 "손실 타수 N타 (OB N회 · 해저드 N회)" 카드를 추가했습니다.
  - **통계 → 추세·기록** 탭의 발전 추세 그래프에 "손실타수" 지표를 추가했습니다 — 스코어·퍼팅·GIR·FIR과 같은 방식으로 최근 라운드 추이(이동평균)·추세 판정(개선 중/주의 필요)·초기 대비 최근 구간 비교가 자동으로 함께 보여집니다.
  - 계산은 매번 그 라운드의 `tpArr`(티샷 결과)에서 직접 하므로, 이 기능 이전에 저장된 라운드도 별도 마이그레이션 없이 바로 표시됩니다.

> 프런트 전용 변경 — `API.VERSION`은 그대로 둡니다. 저장 데이터 스키마는 바뀌지 않았습니다(기존 `tpArr`에서 파생 계산만 추가).

---

## [v12.27.1] - 2026-09-18

### 수정됨
- **티샷 결과 "러프" 칩이 눌러도 켜진 티가 안 나던 버그를 고쳤습니다.** "러프"(페어웨이 미스·벌타 없음)와 "아직 아무것도 안 고름"이 내부 데이터(`fir:false, mulli:0, tp:0`)상 완전히 같은 값이라 구분이 안 됐던 게 원인입니다. 실제 선택 상태를 별도로(`A.sc.teeSel`) 기억해두고, 저장용 `fir`/`mulli`/`tp` 는 그 결과로만 채우도록 고쳤습니다(저장 라운드를 다시 열면 기존처럼 값에서 되짚어 복원 — 이 경우 러프와 미선택은 구분이 안 되므로 미선택으로 둡니다. 무해합니다).
- **"온그린까지"·"퍼팅" 숫자가 검정색이라 다크 테마에서 안 보이던 문제를 고쳤습니다.** 색을 명시하지 않아 기본값(검정)으로 렌더링되고 있었습니다 — 밝은 글자색을 명시했습니다.

### 변경됨
- **홀 진행바를 얇은 선 대신 1~18 번호 버튼으로 바꿨습니다.** 이전엔 어느 홀인지 알아보기 어려웠는데, 이제 번호를 탭해서 바로 그 홀로 이동할 수 있고 크기도 키웠습니다.
- **현재 홀 번호를 큼직하게 강조**했습니다("1번 홀"이 본문 제목 크기로).
- **"이전 홀" 버튼에 글자 라벨을 붙였습니다**(기존엔 "◀" 아이콘만 있어 눈에 안 띄었음). "저장·다음 홀" 버튼 옆에 나란히 배치.

> 프런트 전용 변경 — `API.VERSION`은 그대로 둡니다. 저장 데이터 스키마는 이번에도 바뀌지 않았습니다.

---

## [v12.27.0] - 2026-09-18

### 변경됨
- **스코어 입력 화면을 한 홀씩 전체화면으로 입력하는 방식으로 전면 개편.** 기존엔 전·후반 9홀을 한 화면 리스트로 보여주고 스코어를 －/＋로 직접 조정했는데, 이제 홀마다 전체화면으로 넘어가며 아래 항목을 입력합니다.
  - **"온그린까지 타수" + "퍼팅 수"를 입력하면 스코어(=두 값의 합)와 GIR(온그린 타수 ≤ 파−2)이 자동으로 계산됩니다.** 스코어를 더 이상 직접 두드리지 않습니다 — 18홀을 다 돌면 파 합계가 자연스럽게 기록됩니다.
  - **홀파가 화면에 미리 표시됩니다.** 코스에 저장된 파(파3/4/5)가 기본 선택돼 있고, 탭 하나로 바꿀 수 있습니다. 바꾸면 이 라운드에 바로 적용되고, 마스터 값과 다르면 공식 코스 데이터에도 함께 반영됩니다("⛳ 파수정"과 동일한 방식).
  - **티샷 결과를 순환 버튼 대신 목록에서 선택합니다.** 페어웨이 / 러프 / 해저드 / OB / 멀리건 중 하나를 고르면 되고, 페어웨이를 고르면 FIR이 자동으로 켜집니다. 해저드·OB는 내부적으로 구분해 저장하지만(진단 통계는 기존과 동일하게 둘을 합쳐 "티샷 사고"로 집계), 라운드 상세에서는 구분해서 보여줍니다.
  - 벌타는 여전히 자동으로 더해지지 않습니다 — 실제 벌타는 "온그린까지 타수"에 직접 포함해서 입력해야 합니다. 사용설명서(③·④)를 새 입력 방식에 맞게 다시 썼습니다.
  - 저장된 라운드를 조회하는 읽기 전용 화면(9홀 리스트)은 그대로 유지됩니다.

> 라운드 저장 스키마(`scores`/`puttsArr`/`girArr`/`firArr`/`mulliArr`/`tpArr`/`holePars`)는 바뀌지 않았습니다 — 입력 방식만 바뀐 것이라 서버 저장 형식·CSV 내보내기·통계 계산·`tests/data-safety.test.js`는 그대로 호환됩니다. 온그린 타수는 별도로 저장하지 않고, 라운드를 다시 열 때 "스코어−퍼팅"으로 역산해 복원합니다. 프런트 전용 변경 — `API.VERSION`은 그대로 둡니다.

---

## [v12.26.1] - 2026-09-08

### 변경됨
- **티샷 사고 버튼 이름 정정**: 스코어카드의 티샷 사고 버튼이 순환하며 보여주던 `벌타+1` 표기를 **`티샷 패널티`** 로 바꿨습니다. 이 버튼은 벌타를 스코어에 자동으로 더해주지 않는데(`벌타+1`이라는 이름이 자동 가산으로 오해하기 쉬웠음), 실제 벌타(예: 해저드 ＋1타, OB ＋2타)는 타수 입력(－/＋)에서 직접 넣어야 합니다. 라운드 상세의 홀별 표시 문구도 함께 정정했습니다.
- **사용설명서(③ 홀 파 확인) 문구 정정**: "이 라운드에만 적용, 공식 데이터는 안 바뀜"이라는 설명이 실제 동작과 달랐습니다. 홀파 피커나 "⛳ 파수정"에서 조정한 값은 마스터와 다르면 **공식 코스 데이터에도 함께 저장되어 다른 사용자·관리자에게도 공유**됩니다(`persistParsToOfficial`). 이 사실을 반영해 문구를 고쳤습니다.
- **사용설명서(④ 버튼 의미) 보강**: 티샷/멀리건/티샷 패널티 버튼 설명에 "벌타는 자동으로 더해지지 않고 스코어 입력에서 직접 넣어야 한다"는 안내를 추가했습니다.

> 프런트 전용 변경(버튼 라벨·안내 문구) — `API.VERSION`은 그대로 둡니다. 저장 데이터·계산 로직은 건드리지 않았습니다.

---

## [v12.26.0] - 2026-09-06

### 추가됨
- **📱 안드로이드에서 진짜 "앱"으로 설치됩니다.** 홈 화면에 추가하면 아이콘 구석에 **크롬 배지**가 붙고 주소창 있는 브라우저로 열리던 문제를 고쳤습니다. 원인은 **PWA 매니페스트가 아예 없었던 것** — 크롬이 설치가 아니라 단순 북마크 바로가기로 처리하고 있었습니다.
  - **`manifest.json` 신설** — 앱 이름(온그린), `display: standalone`(주소창 없는 전체 화면), 시작 URL, 세로 고정, 테마색(`#1d9345`)을 선언합니다.
  - **앱 아이콘 파일 4종 추가** — `icon-192.png` / `icon-512.png`(일반)와 `icon-maskable-192.png` / `icon-maskable-512.png`(안드로이드 적응형). 마스커블은 기기가 아이콘을 원·스쿼클로 잘라내도 로고가 안 잘리게 중앙 80% 안에 넣고 초록 배경으로 여백을 줬습니다. 기존 아이콘 이미지를 그대로 썼습니다.
  - `index.html`에 매니페스트 링크와 `theme-color`·`mobile-web-app-capable` 메타를 추가했습니다.

### 변경됨
- **`sw.js`를 자동 해제(kill-switch)에서 "통과형 서비스 워커"로 교체.** 안드로이드 크롬은 매니페스트만 있고 서비스 워커가 없으면 여전히 설치로 인정하지 않기 때문입니다(자매 앱 DKMK도 같은 방식). `index.html`이 서비스 워커를 강제 해제하던 코드도 등록 코드로 바꿨습니다.
  - **캐시는 전혀 하지 않습니다.** 예전 *캐싱* 서비스 워커가 GitHub Pages 재배포 순간의 404를 계속 물고 있어 폐기했던 사고(v12.2.2)를 되풀이하지 않도록, 요청을 저장하지 않고 네트워크로 그대로 흘려보내기만 합니다. 활성화될 때 옛 캐시가 남아 있으면 마저 비웁니다.
- `CLAUDE.md`의 "새 서비스 워커를 도입하지 마세요" 항목을 실제 구조에 맞게 고쳤습니다 — 금지 대상은 **캐싱**이지 서비스 워커 자체가 아님을 명확히 했습니다.

> 프런트 전용 변경 — `API.VERSION`은 그대로 둡니다. 저장 데이터·계산 로직은 전혀 건드리지 않았습니다.
> **⚠️ 홈 화면에 이미 추가해 둔 아이콘은 자동으로 앱으로 바뀌지 않습니다.** 기존 아이콘을 지우고 다시 추가해야 "앱 설치"로 잡힙니다.

---

## [v12.25.0] - 2026-08-18

입력값은 지금 그대로 두고(점수·FIR·멀리건·벌타·파), 드라이버 진단을 더 실질적으로 바꾸고 티샷 버튼을 알아보기 쉽게 다듬었습니다.

### 추가됨
- **🚗 티샷 손실(타/R) — "내 티샷이 한 라운드에 몇 타를 깎아먹나".** 페어웨이를 **지킨 홀**과 **놓친 홀**의 파 대비 평균을 각각 구해 그 차이를 내고, 라운드당 놓친 홀 수를 곱합니다. 퍼센트로는 안 보이던 **실제 손해 타수**가 숫자로 나옵니다.
  - 드라이버 카드에 `페어웨이 52% · 티샷 손실 2.4타/R` 처럼 함께 표시되고, 진단 문구가 `페어웨이를 지킨 홀은 파 대비 +0.3타, 놓친 홀은 +1.0타 — 티샷 하나가 홀당 0.7타, 라운드당 약 2.4타를 깎아먹고 있어요` 로 구체화됩니다.
  - **티샷을 놓쳐도 스코어가 안 무너지는 경우**엔 반대로 알려줍니다 — 그때는 타수가 새는 곳이 드라이버가 아니라는 뜻이라, 엉뚱한 연습을 막아줍니다.
  - **약점 우선순위**의 드라이버 손실 타수도 이 실측값을 쓰도록 바꿨습니다(예전엔 OB/해저드 홀 수로 추정). 표본이 모자라면(지킴·놓침 각 5홀 미만) 표시하지 않고 예전 방식으로 물러납니다.
  - 파4·5홀만 계산하고, 등급(🟢🟡🔴) 판정 기준은 **바꾸지 않았습니다** — 기존 페어웨이% 기준 그대로입니다.

### 변경됨
- **🎨 티샷 사고 버튼을 뜻이 바로 보이게 바꿨습니다.** 예전엔 꺼져 있을 때도 `M` 이라 적혀 있어 **켜짐/꺼짐도, M·TP 의 뜻도** 헷갈렸습니다. 이제 누를 때마다 글자가 바뀝니다.
  - `티샷`(사고 없음·회색) → `멀리건`(다시 침·벌타 없음·**주황**) → `벌타+1`(OB·해저드로 1타 더·**빨강**)
  - 심각도에 맞게 색을 정리했습니다. 예전엔 멀리건이 빨강, 벌타가 주황이라 **더 나쁜 쪽이 덜 위험해 보였습니다.**
  - 홀 상세와 도움말 문구도 `멀리건 (다시 침 · 벌타 없음)` / `벌타 +1 (OB·해저드)` 로 통일하고, 스코어카드 하단 요약 라벨을 `M/TP` → `멀리건/벌타` 로 바꿨습니다.

> 프런트 전용 변경 — `API.VERSION`은 그대로 둡니다. 저장되는 입력값(scores·firArr·mulliArr·tpArr·holePars)의 형식은 **전혀 바뀌지 않아** 기존 기록도 그대로 계산됩니다.

---

## [v12.24.1] - 2026-08-16

### 수정됨
- **📍 저장하거나 점수를 눌러도 보던 홀 위치가 유지됩니다.** 스코어카드는 화면을 다시 그릴 때 홀 목록(`.body-sc`)의 `innerHTML`을 통째로 갈아끼우는데, 그러면 **스크롤이 맨 위로 튕겨서** 8·9번 홀을 치다가 저장하면 1번 홀이 보였습니다. 이제 다시 그리기 전 스크롤 위치를 기억했다가 그대로 되돌려 놓습니다(저장·점수 입력·퍼팅/GIR/FIR/M·TP 모두 해당).
- 전반↔후반을 바꾸거나 라운드를 새로 열 때는 반대로 **첫 홀부터** 보이도록 스크롤을 맨 위로 되돌립니다.

> 프런트 전용 변경 — `API.VERSION`은 그대로 둡니다.

---

## [v12.24.0] - 2026-08-11

### 추가됨
- **💾 스코어 자동 저장.** 점수·퍼팅·GIR·FIR·M/TP를 누를 때마다 **기기에 즉시 저장**되고, 잠깐 모았다가(2초) 서버로 보냅니다. 그래서 라운드 중 앱이 꺼지거나 배터리가 나가도 **입력한 기록이 사라지지 않습니다.** 서버 전송 전에 앱이 종료돼도 다음 실행 때 자동으로 되살아납니다(미동기화 대기목록에 라운드가 통째로 남음).
  - 코스 조합 위쪽에 **저장 상태**가 작게 표시됩니다 — `기기에 저장됨` → `자동 저장됨 ✓`. 인터넷이 없으면 `연결되면 자동 동기화`로 알려줍니다.
  - 앱을 가리거나(홈 화면·화면 잠금) 닫으면 대기 중인 자동저장을 **즉시 전송**합니다.
  - +/- 를 연타해도 서버 요청이 쏟아지지 않도록 전송만 모아 보내고, 기기 저장은 매번 즉시 합니다.

### 변경됨
- **✅ 저장 버튼을 눌러도 스코어카드 화면에 머무릅니다.** 예전엔 저장 후 곧바로 홈으로 나가서, 이어서 입력하려면 라운드를 다시 열어야 했습니다. 이제 저장만 되고 화면은 그대로라 계속 입력할 수 있습니다. 나가려면 **뒤로** 버튼을 쓰면 됩니다.
  - 저장 후에도 같은 라운드를 계속 수정하도록 라운드 id를 유지해 **같은 라운드가 두 개로 늘어나지 않습니다.**
  - 뒤로 나갈 때, 이미 "저장"을 누른 라운드는 **임시저장으로 되돌아가지 않습니다**(기존 상태 유지).

### 수정됨
- 홀 파 수정(🔧 파수정)을 적용하면 그 라운드에 박제되는 파도 함께 자동 저장되도록 했습니다.

> 프런트 전용 변경 — `API.VERSION`은 그대로 둡니다. 자동 테스트 2건(자동저장 복원·정식저장 전환)을 `tests/data-safety.test.js`에 추가해 총 19건입니다.

---

## [v12.23.0] - 2026-08-11

전체 QAQC(데이터 무결성 점검)에서 찾은 **기록 유실·오염 위험**을 모두 막고, 재발 방지용 자동 테스트를 추가했습니다.

### 수정됨
- **🛡️ 오프라인에서 고친 라운드가 조용히 옛 값으로 되돌아가던 문제 (가장 위험).** 예전 병합 로직은 "서버에 **없는** id"만 로컬에서 보존해서, 서버에 이미 있는 라운드를 오프라인에서 수정했다가 동기화가 실패하면 다음 로그인 때 **서버의 옛 값으로 아무 경고 없이 덮어써졌습니다.** 같은 이유로 **오프라인에서 지운 라운드가 되살아나기도** 했습니다. 이제 서버에 못 올린 변경(신규·수정·삭제)을 `og_pending`에 기록해 두고, 다음 로드 때 서버 값 위에 다시 얹어 복원한 뒤 자동으로 재동기화합니다.
- **🔒 저장이 겹칠 때 최신 기록이 되돌아가던 경쟁 상태.** 서버 저장이 전체 덮어쓰기라, 임시저장·삭제·저장이 동시에 날아가면 **늦게 도착한 옛 스냅샷이 최신 저장을 덮어쓸** 수 있었습니다. 이제 저장 요청을 한 줄로 세워(직렬화) 순서를 보장하고, 각 요청은 자기 차례에 최신 상태를 다시 읽습니다. 서버 반영이 확인된 대기분만 정리해, 전송 도중 생긴 새 변경이 유실되지 않습니다.
- **🗑️ 같은 이름 골프장이 함께 삭제되던 문제.** 삭제가 이름 기준이라 동명의 코스가 있으면 **둘 다 사라질** 수 있었습니다. 이제 로컬 삭제는 id로 정확히 한 개만 지웁니다.
- **✏️ 코스 수정이 엉뚱한 골프장을 덮어쓰던 문제.** 수정 저장 시 목록에서 `이름이 예전 이름 **또는** 새 이름`인 항목을 찾아, 개명 시 **동명의 다른 코스**를 덮어쓸 수 있었습니다. 이제 id를 우선해 매칭합니다.
- **🚫 이름 중복 자체를 막습니다.** 골프장 이름이 겹치면(코스를 이름으로 찾는 곳이 있어 파가 엉뚱한 코스에 저장될 수 있음), 그리고 한 골프장 안에서 코스(나인) 이름이 겹치면(두 번째 나인의 파 수정이 조용히 무시됨) 등록·수정을 막고 안내합니다.
- **🧹 이전 코스의 파 수정값이 새 코스로 새던 경로.** 코스 조합 모달이 "새 골프장 등록 직후"에도 열리는데 그 경로엔 초기화가 없어, 직전 코스에서 만진 홀파 수정값이 남아 **이름이 같은 나인이 있으면 새 코스에 잘못 반영**될 수 있었습니다. 이제 모달을 열 때 항상 초기화합니다.
- **⏱️ 응답이 영영 안 와서 "저장 중..."에 갇히던 문제.** 통신에 20초 제한 시간을 두어, 넘으면 네트워크 실패로 처리되고 변경은 대기목록에 안전하게 남습니다.
- **🔑 로그아웃 시 미동기화 변경 경고.** 서버에 못 올린 변경이 남아 있으면 몇 건인지 알려주고 확인을 받습니다. 다른 계정으로 바꿔도 이전 사용자의 대기분이 섞이지 않게 함께 정리합니다.
- **🔢 서버가 id를 문자열로 돌려줘도 안전하게 매칭**하도록 라운드 비교를 보강했습니다(같은 라운드가 중복 쌓이는 것 방지).

### 추가됨
- **🧪 자동 테스트 `tests/data-safety.test.js`** (`node tests/data-safety.test.js`). 설치할 것 없는 순수 Node 스크립트로, `app.js` 소스에서 해당 함수를 직접 뽑아 **17가지**를 검사합니다 — 오프라인 수정·삭제·신규 보존, 저장 겹침, 깨진 데이터 방어, 라벨 복구, 그리고 **모든 경우에 스코어·퍼팅·GIR·FIR·멀리건·TP가 그대로인지**. 옛 로직으로는 실패하는 것을 확인해 테스트가 실제로 버그를 잡는지 검증했습니다.

### 변경됨
- 서버·로컬 병합 로직을 순수 함수 `mergeRounds()`로 분리해 테스트 가능하게 했습니다(동작 동일).

> 백엔드(`Apps_Script.gs`) 미변경 — `API.VERSION`은 그대로 둡니다. `api.js`의 제한 시간 추가는 통신 규약과 무관합니다.

---

## [v12.22.5] - 2026-08-10

### 수정됨
- **🛠️ 코스 조합이 뒤바뀌어 저장되던 버그 수정 (예: 레이크+오션 → 오션+…).** 저장된 라운드를 다시 열 때 레이아웃(나인) 이름을 `layoutIdx`로 마스터 코스를 역참조해 구했는데, 이 인덱스가 스코어 시작 시 만든 "클론 기준 `[0,1]`"이라 **마스터의 실제 나인 순서와 다르면 엉뚱한 코스로 뒤바뀌던** 문제가 있었습니다(그 상태로 재저장하면 라벨이 오염). 이제:
  - 저장 시 **실제 플레이한 나인 이름을 `layoutNames`로 박제**해 인덱스에 의존하지 않습니다.
  - 다시 열 때 이름을 **`layoutNames` → `courseLbl` → (구버전) 인덱스** 순으로 복원합니다.
  - `roundPars`도 이름 기반 조회를 우선해, 마스터 나인 순서가 바뀌어도 파가 어긋나지 않습니다.
- **🔁 이미 저장된 라운드의 뒤바뀐 라벨 자동 복구(`healRoundLabels`).** 로그인 후 기록을 받으면, 각 라운드의 **박제된 홀별 파(`holePars`)를 마스터 나인의 파와 대조**해 원래 코스 조합을 되찾아 `courseLbl`/`layoutNames`만 바로잡고 서버에도 반영합니다. **점수·퍼팅·GIR·FIR·파(스코어/vs/par)는 전혀 건드리지 않으며**, 파가 유일하게 매칭되지 않으면(그날 파를 손수 고쳤거나 애매하면) 그대로 둡니다.
- 백엔드 미변경 프런트 전용 변경이라 `API.VERSION`은 그대로 둡니다.

---

## [v12.22.4] - 2026-08-01

### 변경됨
- **✏️ → "수정" 라벨.** 골프장 선택 목록의 각 카드에 있던 연필(✏️) 아이콘 버튼을 텍스트 **"수정"** 버튼으로 바꿔, 무엇을 하는 버튼인지 한눈에 보이게 했습니다. 동작은 그대로(코스 전체 수정 폼 열기)입니다. 프런트 전용 변경이라 `API.VERSION`은 그대로 둡니다.

---

## [v12.22.3] - 2026-07-06

### 수정됨
- 통계 진단 카드 헤더에서 지표 값(예: 퍼팅 카드의 `라운드 35.7개 (홀당 2.0) · 3퍼팅 3.7회 · GIR홀 2.2`)이 길 때 왼쪽 제목("퍼팅")이 눌려 두 줄로 짤리던 문제 수정. 이제 제목은 한 줄로 고정(`white-space:nowrap`)되고, 대신 오른쪽 값이 줄바꿈되며 한글 단어가 중간에 끊기지 않도록(`word-break:keep-all`) 처리했습니다.

---

## [v12.22.2] - 2026-07-03

### 변경됨
- **🏷️ 홈 화면 헤더 한 줄 정리.** 홈 화면 왼쪽 상단에서 앱 이름(⛳ 온그린)과 사용자 이름(👤 …)이 두 줄로 쌓여 있던 것을 한 줄로 나란히 표시하도록 바꿨습니다. 헤더만 가로 배치로 바꾸고(통계 화면 등 다른 `.t-col`은 그대로), 백엔드 미변경 프런트 전용 변경이라 `API.VERSION`은 그대로 둡니다.

---

## [v12.22.1] - 2026-06-27

### 수정됨
- **🛡️ 스코어카드 데이터 유실 방지 (중요).** 서버가 일시 오류·이상 응답을 줄 때 `A.rounds`가 빈 배열로 덮이고, 그 상태로 저장이 일어나면 서버 `saveRounds_`(시트를 비우고 다시 씀)에 의해 **그 사용자의 라운드가 통째로 삭제되던** 경로를 차단했습니다.
  - `loadAll`: 서버가 **확실히 성공(ok + rounds 배열)** 일 때만 라운드를 교체하고, 그 외엔 절대 빈 배열로 덮지 않고 기존/캐시 데이터를 보존합니다. 실패 시 캐시도 덮어쓰지 않습니다.
  - `A.loaded` 안전장치 + `pushRounds()` 도입: **서버에서 권위 데이터를 받기 전에는 서버를 덮어쓰지 않습니다.** 그 사이의 입력·삭제는 로컬에 항상 보존되고, 다음 성공 로드 때 자동 동기화됩니다(서버에만 없는 라운드는 id 기준으로 병합·보존).
- **🔄 코스 파 수정이 "다시 안 보이던" 문제 수정.** `api.js`의 GET 요청에 캐시 방지(`_` 캐시버스터 + `cache:'no-store'`)를 추가했습니다. 기존엔 Apps Script GET 응답이 브라우저에 캐시되어, 저장한 코스 파 변경이 앱 재실행 시 옛 값으로 되돌아가거나 옛 데이터가 되살아날 수 있었습니다.
- 백엔드 미변경 프런트 전용 변경이라 `API.VERSION`은 그대로 둡니다.

---

## [v12.22.0] - 2026-06-27

### 추가됨
- **✏️ 일반 사용자도 골프장 목록에서 코스 전체 수정 가능.** 코스 선택 화면(`pg-course`)의 각 골프장 카드에 ✏️ 수정 버튼을 항상 노출했습니다. 누르면 코스 이름·주소·전 나인의 홀별 파를 한 번에 고치는 폼이 열리고, 저장하면 서버 공식 코스 목록에 반영되어 **모든 사용자가 공유**합니다. 기존에는 이 "코스 전체 수정"이 관리자 패널에만 있었습니다.
  - 삭제(파괴적 동작)는 기존대로 **관리자만** 카드 왼쪽 슬라이드로 노출됩니다.
  - 백엔드 미변경 프런트 전용 변경이라 `API.VERSION`은 그대로 둡니다. (서버 `saveCourse`가 비관리자 저장을 허용하는지는 일반 계정 1회 실측 권장 — README상 "인증 필요" 등급이라 허용 가능성이 큼)

---

## [v12.21.0] - 2026-06-27

### 변경됨
- **🗺️ 홀파 수정이 이제 공식맵(모두 공유)에 저장됩니다.** 사용자·관리자 구분 없이, 골프장 선택 중(코스 조합 picker)이든 스코어 입력 중(🔧 파수정)이든 홀별 파를 고치면 그 결과가 서버 공식 코스 목록에 반영되어 **모든 사용자가 공유**합니다. 기존에는 "이 라운드에만" 적용되고 마스터는 건드리지 않았습니다.
  - 안전장치: ① **이미 저장된 과거 라운드의 점수·통계는 그대로 유지**됩니다(라운드마다 그날의 파를 `holePars`로 박제해 두기 때문). ② 스코어 입력 중 코스는 선택한 2개 나인만 가진 클론이라, 저장 시 **마스터 전체 코스를 찾아 해당 나인의 파만 이름 매칭으로 갈아끼워** 다른 나인이 사라지지 않게 합니다. ③ 마스터와 실제로 다를 때만 저장하고, 공유 저장이 실패해도(인터넷 등) **이 라운드 입력은 계속 진행**됩니다.
  - 비관리자가 파를 바꾸면 관리자에게 가던 알림(`reportParChange`)은 **변경 이력(감사 로그)으로 유지**하되, 이제 실제로 반영되므로 문구에 `[반영됨]`을 덧붙입니다.
  - 백엔드(`Apps_Script.gs`)는 건드리지 않은 프런트 전용 변경이라 `API.VERSION`은 그대로 둡니다.

---

## [v12.20.1] - 2026-06-26

### 변경됨
- **📱 앱(홈 화면) 아이콘 교체.** 새 "ON GREEN" 마스코트(골프 깃발 + 골든두들 강아지) 이미지로 `apple-touch-icon`과 `favicon`을 교체했습니다. 프런트 전용 에셋 변경이라 `API.VERSION`은 그대로 둡니다.

---

## [v12.20.0] - 2026-06-26

### 변경됨
- **🚦 진단 신호등 기준값(BENCH)을 실제 아마추어 데이터에 맞춰 현실화.** 기존 기준이 싱글~스크래치급으로 너무 빡세서(특히 GIR 🟢 50%·스크램블 🟢 40%) 보통 아마추어는 초록이 거의 안 떴습니다. 국제 샷트래킹 데이터(Shot Scope·Arccos·Lou Stagner 등)를 평균타수 3구간(**~84 / 85~94 / 95~**)에 매핑해 다음과 같이 조정했습니다.
  - **아이언 GIR** — 🟢 `girGood` 50→**40%** · 🔴 `girBad` 28→**21%**
  - **숏게임 스크램블** — 🟢 `scrGood` 40→**34%** · 🔴 `scrBad` 25→**21%**
  - **드라이버(페어웨이%·FIR)는 그대로 유지** — 실력대별 차이가 작은 지표라 데이터상 더 벌릴 여지가 없고, 변별은 OB/해저드 강등이 담당합니다.
- **🍩 퍼팅 등급 판정 방식 교체 — "GIR홀 퍼팅" → "라운드 퍼팅 + 3퍼팅 빈도".** 아마추어 체감(홀당 평균 퍼팅·3퍼팅이 스코어에 직결)에 맞춰, **라운드 퍼팅 수(홀당 평균)** 와 **3퍼팅 빈도** 중 *나쁜 쪽*으로 등급을 정합니다. 거리감이 좋아도 3퍼팅이 잦으면 등급이 내려갑니다.
  - **기준값 교체** — `gpGood`/`gpBad` 제거, `puttGood` 32→**33**·`puttBad` **36**(라운드 퍼팅), `threeGood` **2**·`threeBad` **3.5**(3퍼팅 회/라운드) 신설.
  - **GIR홀 퍼팅(순수 퍼팅력)** 은 등급에선 빠지고 카드·통계에 *참고용 숫자*로만 남깁니다.
  - 퍼팅 신호등 카드 문구, 계산식 안내문(`benchFormulaHTML`), 관리자 설정 입력칸·저장 항목, 통계 지표 설명, 분석 철학 카드 문구를 모두 새 방식에 맞춰 갱신했습니다.

> 참고: 이번 변경은 **프런트 전용**입니다. `api.js`의 `API.VERSION`은 건드리지 않았습니다.

## [v12.19.0] - 2026-06-26

### 변경됨
- **🚗 드라이버 진단의 주지표를 "생존율" → "페어웨이%(FIR)"로 변경.** 생존율은 OB·해저드만 안 나면 100%가 돼 티샷 *정확도*를 제대로 못 재는 한계가 있었습니다. 이제 등급은 **페어웨이%(M·TP로 살린 홀 제외, 파4·5홀 기준)** 로 판정하고, OB/해저드(M+TP)가 잦으면 한 단계 강등하는 기존 보정은 유지합니다.
  - **생존율은 보조 지표로 강등** — 등급에는 쓰지 않고 카드에 숫자로만 함께 보여줍니다(공을 잃지 않은 비율의 참고용).
  - **기준값(BENCH) 교체** — `survGood`/`survOk` 제거, `firGood`(좋음) **60%** · `firOk`(양호) **35%** 신설. 관리자 설정 화면·저장 로직(`ids`)·계산식 안내문도 함께 갱신했습니다.
  - 드라이버 카드 표시를 `페어웨이 N% (생존 N% · OB/해저드 N홀)` 순으로 바꾸고, 진단 메시지·철학 카드·M/TP 입력 도움말 문구를 페어웨이% 중심으로 다듬었습니다.

## [v12.18.1] - 2026-06-24

### 수정됨
- **🚑 통계 화면이 아무것도 안 뜨던 회귀 수정.** v12.18.0에서 통계 하위 탭을 도입할 때 `renderStat`이 쓰는 상태 변수 **`_statSub`·`STAT_SUBS`와 `setStatSub` 함수 선언이 누락**돼, 통계 탭을 열면 `renderStat`이 `ReferenceError`로 중단되어 화면이 빈 채로 남았습니다. 누락된 선언을 `renderStat` 앞에 추가해 해결했습니다. (구문 검사로는 안 잡히는 런타임 참조 오류였습니다.)

## [v12.18.0] - 2026-06-24

### 변경됨
- **📊 통계 화면을 하위 탭으로 정리 — 가독성 개선.** 한 화면에 12개 섹션이 끝없이 늘어지던 [전체] 통계를 **`스코어 · 정확도·퍼팅 · 진단 · 추세·기록`** 4개 하위 탭으로 나눠 한 번에 한 묶음만 보여줍니다. **📋 요약 대시보드(총 라운드·베스트·평균·추정핸디)는 하위 탭 위에 항상 고정**되어 어느 탭에서나 핵심 숫자가 보입니다. **지표·분석값은 하나도 빠지지 않고** 위치만 재배치했습니다.
- **🔍 "이 라운드 분석"도 같은 하위 탭 구조로 통일.** 라운드 상세의 분석을 통계와 동일하게 `스코어 · 정확도·퍼팅 · 진단` 탭으로 정리하고, 그 라운드 한 판 기준으로 계산합니다(여러 라운드가 있어야 의미 있는 **발전 추세·개인기록**, 한 판은 의미 없는 **기복·요약 대시보드**는 제외). **💊 오늘의 처방은 진단 탭 맨 마지막**에 둡니다.
- **🟢 라운드별 목록을 신호 칩으로.** 작은 점(dot)으로 표시하던 FIR·GIR·퍼팅을 **🚗FIR·🎯GIR·🍩퍼팅 신호 칩(🟢🟡🔴)**으로 바꿔, 그 라운드가 어디가 좋고 나빴는지 한눈에 보이게 했습니다.
- **🟢 분석 철학 페이지를 카드형으로.** 줄글 문단이던 ①②③④와 안내를 **아이콘 + 굵은 한 줄 제목 + 흐린 설명 카드**로 바꿔 스캔하기 쉽게 했습니다.
- **📖 통계 지표 설명서에 한 줄 요약(TL;DR) 추가.** 그리고 "파 종류별 (부서 교차)"의 **`(부서 교차)` 문구를 삭제**해 통계·라운드 분석·설명서에서 모두 `파 종류별`로 통일했습니다.
- **📣 업데이트 소식에 한 줄 요약을 추가**하고 이번 변경(탭 정리)에 맞춰 본문을 갱신했습니다.

> 프런트 전용 변경이라 `app.js`의 `APP_VERSION`만 올리고 `api.js`의 `API.VERSION`은 그대로 둡니다(백엔드 `Apps_Script.gs` 미변경).

## [v12.17.2] - 2026-06-23

### 수정됨
- **🔍 홀 상세 보기가 실제로 안 열리던 문제 수정.** v12.17.0에서 홀 상세를 "읽기 전용 스코어카드"에만 달았는데, 스코어카드는 항상 수정 모드로만 열려(읽기 전용 경로 미사용) 탭해도 아무것도 안 떴습니다. 이제 **라운드 상세 모달의 "홀별 스코어" 격자에서 홀을 누르면** 그 홀의 점수·파 대비·FIR·GIR·퍼팅·티샷 사고를 보여줍니다. `holeDetail`을 저장된 라운드 데이터 기준으로 동작하도록 바꿨습니다.

## [v12.17.1] - 2026-06-23

### 변경됨
- **💥 큰 실수 원인 — 티샷 사고에서 멀리건 제외.** "🚗 티샷 사고(OB·해저드)" 집계를 **벌타(TP)만** 세도록 바꿨습니다. 멀리건은 벌타가 더해지지 않으므로 티샷 사고로 카운트하지 않습니다. (멀리건으로 살린 뒤 그린을 놓친 더블보기↑ 홀은 이제 "🎯 그린 미스"로 잡힙니다.)

## [v12.17.0] - 2026-06-23

### 추가됨
- **🔍 홀 상세 보기(읽기 전용 스코어카드).** 저장된 라운드를 열어 본 스코어카드에서 홀을 누르면, 그 홀이 **몇 번 홀인지(파 포함)**, 점수와 파 대비(버디/파/보기 등), **FIR(티샷)·GIR(그린)·퍼팅 수·티샷 사고(멀리건/벌타)** 등 내가 그 홀에 기록한 값을 한눈에 모아 보여주는 모달을 띄웁니다(`holeDetail`, 모달 `m-hd`).

## [v12.16.1] - 2026-06-23

### 변경됨
- **🍩 파 종류별 카드 퍼팅 이모티콘 통일.** 파 종류별 카드의 퍼팅 지표 이모티콘을 앱 전체에서 쓰는 퍼팅 표시(🍩)로 맞췄습니다.
- **나열 순서 FIR → GIR로 정리.** 파 종류별 카드의 지표 순서를 FIR · GIR · 퍼팅 순으로 바꿨습니다.
- **스코어 입력 순서도 FIR 먼저.** 스코어카드 홀 입력의 토글 순서를 FIR → GIR → 퍼팅 순으로 바꿨습니다.

## [v12.16.0] - 2026-06-23

### 추가됨
- **🎯 라운드별 분석에 "파 종류별 (부서 교차)" 추가.** 그동안 전체 통계에만 있던 파3·4·5별 파 대비 + GIR·FIR 교차 진단을, 라운드 상세의 "🔍 이 라운드 분석"에서도 그 라운드 한 판 기준으로 볼 수 있습니다.
- **🥏 파 종류별 카드에 퍼팅 수 추가.** 기존엔 GIR·FIR만 있던 파 종류별 카드에 파3·4·5 각각의 홀당 평균 퍼팅 수를 함께 표시합니다(전체 통계·라운드별 분석 공통).

### 변경됨
- **💊 오늘의 처방을 라운드별(이 라운드) 분석으로 이동.** 전체 통계 화면이 아니라, 라운드 상세의 "🔍 이 라운드 분석"에서 그 라운드의 가장 큰 약점 한 곳을 콕 집어 보여줍니다. 전체 통계에서는 처방을 빼고 약점 우선순위만 유지합니다.

### 제거됨
- **🔄 멀리건 추정 이득 기능 삭제.** 라운드 상세·전체 통계에 있던 멀리건 이득 추정(멀리건 없이 쳤다면 몇 타) 카드를 제거했습니다(`mulliGain`/`mulliGainHTML`/`overallMulliHTML`/`MULLI_GAIN`).
- **🎯 스코어 잠재력 삭제.** 전체 통계의 "베스트 전반+후반" 잠재력 카드를 제거했습니다(`potentialHTML`).

## [v12.14.0] - 2026-06-23

### 추가됨
- **🟢 분석 철학 페이지 + 라운드 탭 배너.** 라운드 탭 맨 위에 "온그린은 이렇게 분석해요" 배너를 띄우고, 누르면 온그린이 통계를 보는 큰 그림(① 원인 진단 ② 두 잣대 ③ 과거의 나와 경쟁 ④ 순수 실력)을 한 페이지로 보여줍니다.
- **💊 오늘의 처방.** 통계(전체) 화면에서 4부서(드라이버·아이언·숏게임·퍼팅) 중 손해가 가장 큰 한 곳을 콕 집어, 무엇을 연습할지 한 줄로 안내합니다(약점 우선순위의 1위를 행동 지시로 강조).
- **💥 큰 실수(블로업)의 원인 분해.** 더블보기 이상 홀이 티샷 사고(OB·해저드)·3퍼팅·그린 미스 중 무엇 때문이었는지 원인별로 집계해 막대로 보여줍니다(한 홀에 원인이 겹칠 수 있어 합계와 다를 수 있음).
- **파 종류별 × 부서 교차.** 기존 "파 종류별(파 대비)"을 확장해, 파3은 GIR / 파4·5는 FIR·GIR을 파 대비와 함께 보여줘 어느 홀 유형에서 어느 부서가 약한지 교차 진단합니다.
- **🎯 스코어 잠재력.** 베스트 전반 + 베스트 후반을 합쳐 "이미 칠 수 있는 점수"를 계산하고, 현재 평균과의 차이(대부분 기복)를 보여줍니다.
- **📈 기복 추세.** 발전 추세 그래프에 '기복' 지표를 추가했습니다. 최근 5R 스코어 편차의 흐름으로 점수가 점점 일정해지는지 봅니다.
- **🔄 멀리건 추정 이득.** 멀리건(M) 1회 ≈ 약 1타 이득으로 보수적으로 추정해, 멀리건 없이 쳤다면 점수가 얼마였을지 라운드 상세(라운드별)와 통계(전체)에서 보여줍니다. TP(벌타 받고 진행)는 이미 점수에 반영돼 제외합니다.

### 비고
- 모두 **프런트 전용** 변경입니다. 메모리의 `A.rounds` 로 즉시 계산하며 백엔드(`Apps_Script.gs`)는 건드리지 않았으므로 `api.js`의 `API.VERSION`은 그대로 둡니다.

## [v12.13.1] - 2026-06-23

### 수정됨
- **마지막 업데이트 후 앱이 전혀 안 열리던(접속 불가) 치명적 버그 수정.** v12.13.0 머지 과정에서 `app.js` 상단에 `const APP_VERSION` 선언이 두 줄(`v12.13.0`, `v12.12.2`) 남아, 같은 `const`를 두 번 선언하는 `SyntaxError`가 발생했습니다. 이 때문에 `app.js` 전체가 파싱되지 않아 로그인 화면조차 뜨지 않았습니다. 중복 선언을 제거해(`v12.13.0` 하나만 유지) 정상화했습니다. 서버 버전 불일치(VER 에러)가 아니라 프런트 스크립트가 통째로 죽은 문제였습니다.

## [v12.13.0] - 2026-06-23

### 추가됨
- **관리자 ‘공식 골프장 관리’에서 검색·전체 목록의 총 개수 표시.** 골프장을 검색하면 결과가 몇 개인지 알 수 없었는데, 이제 목록 위에 “🔍 검색 결과 N개” / “전체 N개”로 총 개수를 보여줍니다.
## [v12.12.2] - 2026-06-23

### 수정됨
- **관리자 ‘공식 골프장 관리’에서 검색이 한 번에 되도록 수정.** 예전에는 검색어를 입력해도 목록을 아직 안 불러왔으면 결과가 안 나와서 `골프장 목록 불러오기`를 누른 뒤 **다시 검색**해야 했습니다. 이제 ①관리자가 설정 화면을 열면 골프장 목록을 자동으로 불러오고(`goSet`), ②불러오는 도중 입력해 둔 검색어가 있으면 불러온 뒤 그 검색어를 그대로 복원해 결과를 바로 보여줍니다(`admLoadOfficial`). 한 번만 검색하면 됩니다.

### 변경됨
- **업데이트 소식 공지에서 ‘(관리자 포함)’ 문구 삭제.** 공지는 관리자·사용자가 함께 보는 글이라 ‘(관리자 포함)’ 표현이 불필요해, 업데이트 소식 본문(`updateNewsHTML`)에서 해당 문구를 뺐습니다.

## [v12.12.1] - 2026-06-23

### 수정됨
- **관리자 ‘공식 골프장 관리’ 검색창에서 한글이 끝까지 입력되지 않던 버그 수정.** 글자를 입력할 때마다 검색창(input)까지 통째로 다시 그려서 한글 조합이 중간에 끊겼고, 그 결과 마지막 글자가 안 써지는 현상이 있었습니다. 이제 검색창은 한 번만 만들고 목록 영역만 갱신하도록 분리해(`renderAdmOffList`) 조합이 끊기지 않습니다. (코스 검색창 `cs-q`는 정적 요소라 같은 문제가 없었습니다.)

## [v12.12.0] - 2026-06-23

### 추가됨
- **내 스코어카드 전체를 구글시트용 CSV로 내보내기.** 설정 화면에 `📥 스코어카드 내보내기` 메뉴를 추가했습니다(모든 사용자). 버튼을 누르면 내 전체 라운드(작성중 임시저장 제외)를 한 CSV 파일로 내려받고, 구글시트에서 `파일 → 가져오기`로 열 수 있습니다. 각 라운드마다 골프장·코스 구성·날짜·날씨·동반자·메모·총타수/파대비·FIR/GIR/퍼팅/멀리건/트러블샷 요약과 함께 **홀 번호(홀1~홀18)와 홀별 파 구성**, 홀별 스코어·퍼팅·GIR·FIR·멀리건·트러블샷을 표로 담습니다(파3 홀의 FIR은 `-`). 한글이 깨지지 않도록 UTF-8 BOM을 붙였습니다. 데이터는 이미 메모리에 있는 `A.rounds`로 만들어 서버를 거치지 않습니다. (`exportScorecards`, `downloadCSV`, `csvCell`)
### 변경됨
- **관리자 ‘사용자’ 목록에 마지막 접속 날짜를 명확히 표시.** 예전에는 사용자 아래에 날짜가 아무 설명 없이 떠서 무슨 날짜인지 알기 어려웠는데, 이제 `🕒 마지막 접속 {날짜}`로 라벨을 붙여 간략히 보여줍니다. 서버가 날짜를 주지 않으면 `기록 없음`으로 표시합니다. (`admLoadUsers`)

> 프런트엔드 전용 변경입니다. `Apps_Script.gs`를 건드리지 않았으므로 `api.js`의 `API.VERSION`은 그대로 둡니다.

---

### 추가됨
- **업데이트 안내 팝업.** 앱이 새 버전으로 업데이트되면, 다음 접속 때 **바뀐 내용이 팝업으로 한 번** 뜹니다(관리자도 동일). 닫으면 같은 버전에선 다시 뜨지 않습니다. 팝업 위·아래에 **현재 버전 번호를 작게** 함께 표시합니다.
  - **공지 글이 쌓이지 않습니다** — 업데이트마다 새 글을 만들지 않고, 단일 **"📣 업데이트 소식"** 글 하나(`updateNewsHTML`)가 늘 최신 변경 내용으로 갱신됩니다. 새 버전을 낼 때 이 함수 본문만 고치면 됩니다.
  - **알림 배지 연동** — 업데이트가 있으면 홈 📢 아이콘에 빨간 알림이 뜨고(`updatePending()`이 `APP_VERSION` 기준으로 판정), **공지 게시판을 한 번 열면 사라집니다**(`markNoticesSeen`이 `og_update_seen`을 현재 버전으로 기록).
  - 상태 키 분리: 첫 설명서 팝업은 종전대로 `og_guide_seen`, 업데이트 팝업 1회 표시는 `og_update_popped`, 배지(읽음 처리)는 `og_update_seen`. 첫 로그인 사용 설명서 팝업과 충돌하지 않습니다(첫 설치 땐 설명서만 뜨고 업데이트 팝업은 생략).

> 프런트엔드 전용 변경입니다. `Apps_Script.gs`를 건드리지 않았으므로 `api.js`의 `API.VERSION`은 그대로 둡니다.

## [v12.11.0] - 2026-06-23

### 추가됨
- **화면을 오른쪽으로 슬라이드하면 뒤로 가기(iOS 스타일).** 뒤로 버튼은 그대로 두고, 코스 선택·스코어카드·설정·공지 화면에서 손가락으로 화면을 오른쪽으로 끌면 끄는 만큼 화면이 따라 밀리고, 화면 폭의 약 1/3 이상 끌면 뒤로 이동합니다. 세로 스크롤이나 골프장 카드의 가로 스와이프(삭제)와 겹치지 않게, 세로 움직임·골프장 카드 위에서 시작한 동작·모달이 떠 있을 때는 동작하지 않습니다. (`initSwipeBack`, 페이지별 뒤로 동작 `BACK_ACTIONS`)
- **아래에서 올라오는 모달은 뒤 배경을 누르면 닫힙니다.** 새 라운드·골프장 추가/수정·코스/홀 선택·홀 파 수정·삭제 확인·라운드 상세·공지 읽기 등 모든 바텀시트 모달에서 시트 바깥(어두운 배경)을 탭하면 취소(닫기)와 동일하게 닫힙니다. (`initModalBackdrop`)

### 변경됨
- **관리자 ‘공식 골프장 관리’ 검색창을 처음부터 표시.** 예전에는 "골프장 목록 불러오기"를 눌러야 검색창이 나타났지만, 이제 설정 화면을 열면 검색창이 바로 보입니다. 목록 데이터는 종전처럼 "불러오기"를 눌러야 받아오고(검색창 옆 버튼으로도 불러올 수 있음), 불러온 뒤 검색·펼침이 동작합니다.
- **관리자가 골프장을 수정·삭제해도 마지막으로 보던 목록(검색어·펼침 상태)을 그대로 유지.** 예전에는 수정/삭제 때마다 서버에서 목록을 다시 불러오며 검색어와 펼친 상태가 초기화됐지만, 이제 로컬 목록만 갱신해 보던 화면을 유지합니다.

> 프런트엔드 전용 변경입니다. `Apps_Script.gs`를 건드리지 않았으므로 `api.js`의 `API.VERSION`은 그대로 둡니다.

## [v12.10.1] - 2026-06-23

### 수정됨
- **설정·관리자 화면을 보다가 자꾸 라운드(홈) 목록으로 튕기던 버그 수정.** 앱을 켜거나 로그인한 직후 `loadAll()`이 백그라운드에서 서버 데이터를 받아오는데, 응답이 도착하면 **현재 보고 있는 화면과 무관하게 무조건 홈으로 전환**해버렸습니다. 그래서 로딩이 끝나기 전에 ⚙️ 설정/관리자 화면을 열면 잠시 뒤 라운드 목록으로 되돌아갔습니다(통계·스코어카드 등 다른 화면도 동일하게 영향). 이제 백그라운드 갱신이 끝나도 **사용자가 보던 화면을 그대로 유지**하고, 홈/로그인 상태일 때만 홈으로 이동합니다. 홈 내용은 뒤에서 조용히 갱신돼 돌아왔을 때 최신입니다. (현재 페이지 판별용 `curPg()` 헬퍼 추가)

> 프런트엔드 전용 수정입니다. `Apps_Script.gs`를 건드리지 않았으므로 `api.js`의 `API.VERSION`은 그대로 둡니다.

## [v12.10.0] - 2026-06-23

### 추가됨
- **🚦 진단 신호등 계산식을 세 곳에서 모두 볼 수 있게 통일.** 같은 설명을 그리는 단일 함수 `benchFormulaHTML()`을 만들어 ①공지(통계 분석 지표 설명), ②관리자/설정의 분석 기준, ③사용자 통계 화면에서 함께 씁니다. 기준값(BENCH)이 바뀌면 세 곳이 동시에 자동 갱신돼 서로 어긋날 일이 없습니다.
  - 통계 화면에는 진단 카드 아래에 **"🧮 진단 신호등 계산식 보기"** 접이식(`<details>`)으로 추가해, 평소엔 접혀 있다가 펼치면 드라이버·아이언·숏게임·퍼팅의 산식과 신호등 기준값을 그대로 볼 수 있습니다.
  - 공지의 "🚦 진단 신호등" 항목을 축약 설명에서 설정·통계와 동일한 전체 계산식으로 교체했습니다.

### 변경됨
- **🚗 드라이버 진단에 페어웨이% 설명 추가.** 드라이버 카드 값에 함께 표시되던 `페어웨이 N%`가 무엇인지 — 티샷이 페어웨이에 떨어진 홀(FIR) ÷ 파4·5홀(M·TP로 살린 홀 제외)이며 **등급 판정에는 쓰지 않는 참고 지표**라는 점 — 을 카드 설명(ℹ️)과 계산식 양쪽에 명시했습니다.

> 프런트엔드 전용 변경입니다. `Apps_Script.gs`를 건드리지 않았으므로 `api.js`의 `API.VERSION`은 그대로 둡니다.

## [v12.9.0] - 2026-06-22

### 추가됨
- **🚦 진단 신호등에 ⛳ 숏게임(스크램블링) 지표 추가 — 3지표 → 4지표.** 그린을 놓친 홀 중 파 이하로 막은 비율을 측정합니다(높을수록 좋음). 기준값 🟢 `scrGood`(기본 40%) / 🟡 `scrBad`(기본 25%). 아마추어 스코어를 크게 좌우하는 쇼트게임을 약점 우선순위뿐 아니라 신호등에서도 바로 진단합니다. 표시 순서: 드라이버 → 아이언 → 숏게임 → 퍼팅.

### 변경됨
- **🍩 퍼팅 등급 판정을 "라운드 총 퍼팅"에서 "GIR홀 퍼팅(순수 퍼팅력)"으로 교체.** 총 퍼팅은 GIR이 낮으면 칩샷 후 원퍼팅으로 오히려 줄고, GIR이 높으면 먼 첫 퍼트가 많아 늘어나는 교란이 있었습니다. 정규로 올린 홀의 홀당 퍼팅(기준값 🟢 `gpGood` 1.9 / 🟡 `gpBad` 2.05개/홀)으로 판정해 그린 적중률과 분리했습니다. GIR홀이 하나도 없는 경우에는 기존 총 퍼팅 기준(`puttGood`/`puttBad`)으로 자동 폴백합니다. 카드에는 GIR홀 퍼팅·총 퍼팅·3퍼팅을 함께 표시합니다.
- 설정 → 📊 분석 기준에 숏게임·GIR홀 퍼팅 기준값 입력 항목 추가, 설명문·통계 가이드도 새 지표에 맞춰 갱신. 기준값 입력칸은 소수 입력(GIR홀 퍼팅)을 위해 `inputmode="decimal"`로 변경.
- 약점 우선순위(스트로크게인)의 숏게임 손실 타수 계산을 `analyze()`가 집계한 스크램블 값으로 일원화(중복 계산 제거).

> 프런트엔드 전용 변경입니다. `Apps_Script.gs`를 건드리지 않았으므로 `api.js`의 `API.VERSION`은 그대로 둡니다. (새 BENCH 키 `gpGood`/`gpBad`/`scrGood`/`scrBad`는 기존 BENCH 저장/병합 경로로 그대로 직렬화되며, 서버에 값이 없으면 위 기본값이 유지됩니다.)

## [v12.8.0] - 2026-06-22

### 추가됨
- **📱 앱 공유 / 초대 (설정 화면).** 설정에 `📤 앱 링크 공유 / 초대` 버튼을 추가했습니다. 현재 접속 주소를 그대로 공유하며, **아이폰은 사파리로 열어 '홈 화면에 추가'**, 안드로이드는 크롬에서 '홈 화면에 추가' 하라는 안내 문구를 함께 보냅니다(휴대폰 공유시트 → 없으면 클립보드 복사).
- **⭐ 기록 보유 라운드 별표 마크.** 개인 기록(베스트 스코어·최소 퍼팅·최고 GIR/FIR·최다 버디·**최소 패널티**)을 가진 라운드 카드에 ⭐ 별표를 붙이고, 어떤 기록인지 이모지 배지로 함께 표시합니다(홈·라운드별·상세).
- **최소 패널티 기록(🛟)** 추가 — 멀리건+TP 합이 가장 적은 라운드(라운드 간 차이가 있을 때만 인정).
- **전체 통계 "실력 비율"에 🕊️ 버디 비율** 추가.
- **라운드 상세에 "이 골프장 이전 기록과 비교"** — 같은 골프장의 이전 평균·베스트와 이번 스코어를 바로 비교(▼좋아짐/▲나빠짐).

### 변경됨
- **버디 아이콘을 🕊️로 통일**(기존 🐦).
- **스코어 입력 시 퍼팅 표기를 `P`로 변경** — `🍩` 대신 `2P·3P…`(기본 2P부터)로 표시.
- **스코어카드 작성 중 파 수정 버튼 아이콘을 🔧로 교체**(기존 ⛳).
- **라운드 목록의 FIR·GIR·퍼팅 글씨색 가독성 개선** — 어둡게 보이던 지표 텍스트에 색을 지정(홈·라운드별 통계 탭).
- **전체 통계 진단 주석줄 가독성 개선** — 단어가 어색하게 끊기지 않도록(`word-break:keep-all`) + 글씨색을 밝게.
- **골프장 슬라이드-삭제 버튼 모양 정리** — 카드를 밀었을 때 생기던 비대칭 모서리를 없애 매끄럽게.
- **공지/설명서 읽기 화면을 더 크게** — '환영합니다' 등 공지를 한 화면 가득 큰 글씨로 표시.
- **+ 골프장 등록 시 막히는 이유를 명확히 안내** — 이름/코스 이름이 비면 그 칸을 빨갛게 표시하고 안내 문구를 띄움.

### 제거됨
- **전체 통계 "정확도의 가치" 섹션 삭제.**
- **전체 통계 "코스별 평균" 섹션 삭제** — 대신 라운드 상세의 코스 비교로 대체.

---

## [v12.7.0] - 2026-06-22

통계 분석 전면 개편 — "과거의 나와 비교한 발전"에 초점. **입력 항목은 그대로**이며, 기존에 쌓인 홀별 데이터만으로 새 분석을 계산합니다(서버 변경 없음, `API.VERSION` 그대로).

### 추가됨
- **📋 요약 대시보드** — 총 라운드·베스트·평균 스코어·**추정 핸디**(최근 20R 중 좋은 라운드의 오버파 평균, 간이) + 초기 대비 발전 한 줄 요약.
- **📈 발전 추세** — 지표(스코어/퍼팅/GIR/FIR)를 골라 보는 라인 그래프 + **5R 이동평균선**, 선형회귀 기반 추세 판정(개선/정체/주의)과 라운드당 변화량, **초기 vs 최근 구간 비교**.
- **🎯 약점 우선순위** — 드라이버·아이언·숏게임·퍼팅을 설정 기준값과 비교해 라운드당 손실 타수를 추정, ⭐최우선부터 고칠 순서 제시(상대 비교용).
- **실력 비율** — 파 이하/보기/더블+ 비율 + 라운드당 **블로업(트리플+)** 홀.
- **퍼팅 분포·1퍼팅율** — 1/2/3/4+ 퍼팅 홀 수 막대 + 1퍼팅 비율.
- **정확도의 가치** — 페어웨이·그린 적중 vs 놓침 홀의 평균 오버파 차이.
- **🏆 개인기록·마일스톤** — 베스트/최소 퍼팅/최고 GIR·FIR/최다 버디 기록 + 100·90·80 첫 돌파 날짜.
- **🏆 트로피 배지** — 개인 기록을 보유한 라운드 카드에 🏆🍩🎯🚗🐦 배지 표시(홈·라운드별 목록·라운드 상세).

### 변경됨
- 타수 분포에서 `더블+`를 **더블 / 트리플+** 로 분리.
- 단순 막대 "스코어 추이"는 **발전 추세** 그래프로 대체.
- 통계 지표 설명(게시판) 본문을 새 구성에 맞춰 자동 갱신.

## [v12.6.2] - 2026-06-22

### 추가됨
- **전체 통계에 스크램블링 설명 추가** — 통계 탭의 **전체** 보기에서 스크램블링 지표 바로 아래에 "그린을 놓친 홀에서 파 이하로 막아낸 비율"이라는 짧은 설명을 옅은 글씨로 표시합니다. 별도 설명서를 열지 않아도 지표의 뜻을 바로 알 수 있습니다.

---

## [v12.6.1] - 2026-06-22

### 변경됨
- 라운드 상세의 **홀별 스코어**를 두 줄로 정리했습니다 — 첫 줄은 전반(1–9홀), 둘째 줄은 후반(10–18홀). 줄바꿈이 코스에 맞게 항상 9홀씩 끊겨 전·후반을 한눈에 비교할 수 있습니다.

---

## [v12.6.0] - 2026-06-22

### 추가됨
- **📢 공지 게시판 (읽기 전용)** — 홈 화면 오른쪽 위 ⚙️ 설정 옆에 **📢 공지** 버튼을 추가했습니다. 사용자는 공지·설명서를 **읽기만** 할 수 있고, **새 글이 올라오면 📢 아이콘에 빨간 알림(NEW) 배지**가 떠서 바로 알 수 있습니다(게시판을 열면 읽음 처리되어 배지가 사라짐).
- **사용 설명서 — 스코어카드 작성** — 게시판에 항상 최신으로 유지되는 사용 설명서를 넣었습니다. 새 라운드 시작 → 골프장 선택(아직 리스트가 완성 중이라 없으면 직접 추가) → **홀 파 구성 확인/수정** → 각 버튼(−/＋·GIR·FIR·🍩 퍼팅·M/TP)의 의미와 이유까지 단계별로 설명합니다. 본문은 함수로 자동 생성되어 **기능이 바뀌면 자동으로 갱신**됩니다(등록 골프장 수·앱 버전 등 실시간 반영).
- **통계 분석 지표 설명** — 진단 신호등(드라이버 생존율·아이언 GIR·퍼팅)부터 핵심 지표·파 종류별·쇼트게임·전반/후반·그래프까지 각 지표가 무엇을 뜻하는지 설명하는 글을 추가했습니다. **분석 기준값(BENCH)을 그대로 끌어와** 기준이 바뀌면 설명도 함께 자동 갱신됩니다.
- **첫 로그인 팝업** — 이 기기에서 처음 로그인하는 사용자에게는 **사용 설명서가 팝업**으로 한 번 뜹니다. 한 번 닫으면 다시 뜨지 않습니다(`og_guide_seen`).

## [v12.5.0] - 2026-06-22

### 추가됨
- **관리자 알림 바로가기** — 라운드(홈) 화면 맨 위 노란 테두리 알림 배너를 누르면, 설정 화면을 거쳐 **관리자 패널의 "골프장 변경 알림" 메뉴로 바로 이동**합니다. 변경 내역을 자동으로 펼쳐 불러오고 그 위치로 스크롤해, 따로 "변경 알림 보기" 버튼을 누르지 않아도 됩니다.

### 변경됨
- **변경 알림 상세 강화** — 사용자가 라운드 중 "⛳ 파수정"으로 홀별 파를 고치면, 관리자에게 가는 알림에 **어느 골프장 · 어느 구성(코스 조합) · 어느 레이아웃 몇 번 홀이 어떻게(P4→P5) 바뀌었는지**까지 담깁니다. 관리자는 알림만 보고도 무엇이 수정됐는지 바로 알 수 있습니다.

### 수정됨
- **앱이 켜지지 않던 치명적 버그 수정** — `app.js`에서 `APP_VERSION`이, `index.html`에서 `style.css`/`app.js`/`api.js` 로드 구문이 각각 **두 번 중복 선언·로드**되어 스크립트가 깨지던 문제를 정리했습니다.

## [v12.2.4] - 2026-06-22

### 수정됨
- 라운드 추가 → 골프장/코스 선택 시, **홀 선택 모달에서 고친 홀별 파가 코스 조합을 바꾸면 사라지던 문제**를 수정했습니다. 이제 수정값을 **코스(레이아웃)별로 보존**하므로, 조합을 바꿨다가 돌아와도 — 또 같은 코스가 들어간 다른 조합에서도 — 고친 파가 그대로 유지됩니다. (마스터 공식 코스 데이터는 그대로 보존)
## [v12.4.0] - 2026-06-22

### 변경됨
- **스코어카드 작성 — 골프장 선택 화면**: 각 골프장 카드의 연필(수정) 버튼과 삭제 버튼을 없앴습니다. 화면이 깔끔해지고 실수로 누르는 일이 줄어듭니다.

### 추가됨
- **슬라이드-삭제**: 골프장 카드를 왼쪽으로 밀면 뒤에서 빨간 "🗑 삭제" 버튼이 나타나도록 했습니다(관리자만). 다른 카드를 밀거나 탭하면 열려 있던 카드는 자동으로 닫힙니다.

---

## [v12.3.1] - 2026-06-22

### 수정됨
- **라운드(홈) 화면 설정 알림 뱃지가 창 밖으로 잘리던 문제 수정** — 관리자 알림이 있을 때 ⚙️ 설정 버튼의 빨간 숫자 뱃지가 상단 바(`overflow:hidden`) 위로 삐져나가 윗부분이 잘렸습니다. 뱃지를 버튼 오른쪽 위 모서리 **안쪽**(`top/right:1px`)으로 옮겨 항상 온전히 보이도록 했습니다.

### 변경됨
- **앱 아이콘(홈 화면)의 흰 테두리를 아주 얇게** — `apple-touch-icon`/`icon`의 PNG에서 흰 여백(약 18~22px)을 4px로 줄여, 가운데 캐릭터 그림과 초록 배경이 더 크게 보이도록 다시 만들었습니다.

> 서버(Apps Script) 변경 없음 — `api.js`의 `API.VERSION`은 `v12-2026.06.21` 그대로 유지.

---

## [v12.3.0] - 2026-06-22

### 추가됨 (Added)
- **저장 완료된 스코어카드 공유.** 라운드 상세 보기에 `📤 스코어카드 공유` 버튼을 추가했습니다.
  공유 텍스트의 지표 순서도 드라이버(FIR) → 아이언(GIR) → 퍼팅으로 통일.
- **관리자 변경 알림에 수정 내용 표시.** 사용자가 홀 파를 수정하면, 관리자 패널의
  변경 알림에 "무엇이 어떻게 바뀌었는지"(예: `1번 P4→P5`)까지 함께 보입니다.
  (프런트는 이미 `detail`을 전송 중 — 서버가 돌려주면 표시, 없으면 안전하게 생략)

### 변경됨 (Changed)
- **연필(✏️) 아이콘은 '작성 시작'을 의미하도록 정리.** 코스 구성 선택 모달의 시작 버튼을
  `✏️ 스코어카드 작성 시작`으로 바꿨습니다. 저장된 라운드를 고치는 진입점(상세 보기·읽기전용
  배너)의 버튼은 연필 대신 `🔧 수정`으로 바꿔 '작성'과 '수정'을 시각적으로 분리했습니다.
  (홀 파 수정은 기존 `⛳ 파수정` 버튼 유지)
- **진단·스코어카드 목록 지표 순서를 드라이버 → 아이언 → 퍼팅으로 통일.**
  통계 진단 카드, 홈/통계 라운드 카드 메타, 라운드 상세 그리드 모두 같은 순서로 정렬.

### 제거됨 (Removed)
- **작성 중 스코어카드 공유 버튼 제거.** 작성 화면 헤더의 `📤` 공유 버튼을 없애고,
  공유는 저장 완료된 라운드(상세 보기)에서만 가능하도록 했습니다.
- **통계(분석) 공유 버튼 제거.** 통계 화면 헤더의 `📤` 공유 버튼과 `shareStat()` 함수를 삭제.

> 서버(Apps Script) 변경 없음 — `api.js`의 `API.VERSION`은 `v12-2026.06.21` 그대로 유지.

---

## [v12.2.3] - 2026-06-22

### 수정됨
- **"버전이 안 맞아요"(VER) 에러 해결.** 앞서 `api.js`의 `API.VERSION`을
  서버와 같은 `v12-2026.06.21`로 되돌렸지만, `index.html`의 캐시 우회 쿼리
  (`api.js?v=...`)가 `APP_VERSION`과 같아 바뀌지 않아 브라우저·CDN이 옛 v13
  `api.js`를 계속 캐시에서 내주고 있었습니다. `APP_VERSION`을 `v12.2.3`으로
  올리고 `style.css`/`api.js`/`app.js`의 `?v=` 쿼리를 모두 `12.2.3`으로 맞춰,
  모든 기기가 고쳐진 `api.js`를 새로 받도록 했습니다. (서버 변경 없음)

---

## [v12.2.2] - 2026-06-22

### 제거됨
- **서비스 워커(오프라인 캐시) 기능을 폐기했습니다.** 단순 정적 사이트에는
  과한 기능이었고, 무엇보다 한 번 등록되면 기기에서 잘 떨어지지 않아 배포 중
  GitHub 404 가 계속 노출되는 원인이 됐습니다.

### 수정됨
- `sw.js` 를 **자동 해제(kill-switch)** 워커로 교체. 옛 서비스 워커가 박힌
  기기가 한 번만 접속하면 SW 등록을 스스로 해제하고 캐시를 모두 비운 뒤
  새로고침해, SW 없는 깨끗한 정적 사이트로 복귀합니다. (fetch 를 가로채지
  않으므로 더 이상 404 를 노출하지 않습니다.)
- `index.html` 에서 SW 등록 코드를 제거하고, 남아 있을 수 있는 옛 등록·캐시를
  정리하는 코드로 교체.

---

## [v12.2.1] - 2026-06-22

### 수정됨
- **서비스 워커가 배포 중 GitHub 404 페이지를 그대로 노출하던 문제 수정.**
  여러 변경을 연달아 배포하면 GitHub Pages가 재배포되는 짧은 순간 "There
  isn't a GitHub Pages site here" 404(HTTP 404)를 돌려주는데, 기존 서비스
  워커는 `fetch`가 *성공*했지만 응답이 404/5xx인 경우 캐시로 폴백하지 않고
  그 에러 페이지를 사용자에게 그대로 보여줬습니다(`try/catch`는 네트워크가
  끊겨 throw될 때만 폴백). 이제 네트워크 응답이 정상(`ok`)이 아니면 캐시에
  저장된 정상 화면으로 폴백하므로, 배포 중에도 홈스크린 앱이 깨지지 않습니다.

---

## [v12.2.0] - 2026-06-22

### 추가됨 (Added)
- 서비스 워커(`sw.js`) 도입 — 사파리 홈스크린(standalone) 앱에서 캐시 때문에 새 버전이 안 보이던 문제 해결.
  - network-first 전략: 온라인이면 항상 서버에서 최신 자산을 받아오고, 오프라인일 때만 캐시로 폴백.
  - 배포(버전 변경)할 때마다 옛 캐시를 자동으로 비우고 새 워커가 즉시 교체됩니다.
- API 호출(외부 출처)은 서비스 워커가 건드리지 않아 통신 동작에는 영향 없음.

### 변경됨 (Changed)
- `index.html` 의 자산 참조(`api.js`/`app.js`/`style.css`)에 버전 쿼리스트링(`?v=`)을 붙여 HTTP 캐시도 우회.

---

## [v12.1.0] - 2026-06-22

### 추가됨 (Added)
- 체인지로그 파일(`CHANGELOG.md`) 신설.
- 프런트엔드 앱 버전 `APP_VERSION` 도입 (서버 통신용 `API.VERSION`과 분리).
- 로그인 화면 하단과 설정 화면 하단에 현재 앱 버전을 옅은 글씨로 표시.

---

## [v12.0.0] - 2026-06-21

### 기준점 (Baseline)
- 이 체인지로그 도입 이전까지의 누적 상태를 v12로 표기합니다.
- 골프 스코어카드 입력, 통계(신호등), 골프장 관리, 관리자 기능, 버전 도장 배너,
  BENCH(분석 기준값) 서버 공유 등 기존 기능 포함.
